# TheBearing.io — Handoff Document
> Last updated: 2026-06-24 | Current build: v75l

---

## Project Overview
TheBearing.io is a curated luxury travel platform. Miguel is founder and works hands-on across product, design, and development. The site is a flat-file HTML/CSS/JS frontend deployed via Cloudflare Pages, backed by a single Cloudflare Worker (`functions/api/envoy.js`) that handles all API endpoints.

---

## Infrastructure

| Item | Value |
|------|-------|
| GitHub repo | `mcancino213/thebearing.io` |
| Deploy | Cloudflare Pages — push to `main` auto-deploys |
| Worker | `functions/api/envoy.js` |
| KV namespace | `DOSSIERS` (id: `aa0c885871474266966e50f0676dd019`) |
| CF Account ID | `d62dd7db798247bb6cc9ff18ff7ee84f` |
| CF Account Hash | `YyCqpmHo4EG6ShyDMCRcVQ` |
| CF Images token | `CF_IMAGES_TOKEN` (secret in worker) |
| Anthropic key | `ANTHROPIC_API_KEY` (secret in worker) |
| Live URL | https://thebearing.io |

## Workflow
1. Miguel describes changes
2. Claude makes them and packages a .zip
3. Claude provides terminal commands to apply the patch
4. Miguel pushes from GitHub Desktop to live

**Never deploy via local `wrangler` CLI** — always push to GitHub and let Cloudflare Pages build. Local Mac environment causes crashes.

---

## Tech Stack

- **Frontend:** Flat HTML/CSS/JS, no build step, no framework
- **Fonts:** Instrument Serif, Cormorant Garamond, Geist
- **CSS variables:** `--cream:#faf7f1`, `--ink:#1e1810`, `--terra:#b05830`, `--gold:#9a7230`, `--sand:#ece5d8`, `--stone:#7a6a58`, `--bark:#5a4a38`, `--moss:#4a6a48`
- **Worker:** Cloudflare Pages Functions (ESM)
- **Storage:** Cloudflare KV (`DOSSIERS` namespace)
- **Images:** Cloudflare Images (`imagedelivery.net/YyCqpmHo4EG6ShyDMCRcVQ/{id}/public`)

---

## Worker API Endpoints (`functions/api/envoy.js`)

| Endpoint | Method | Purpose |
|----------|--------|---------|
| `/api/envoy` | POST | Anthropic API proxy |
| `/api/property` | GET/POST/DELETE | Full property CRUD + index |
| `/api/dossier` | GET/POST | Property dossier text |
| `/api/itinerary` | GET/POST | Day-by-day itinerary JSON |
| `/api/source` | GET/POST/DELETE | URL fetch + KV cache for dossier sources |
| `/api/upload` | POST | Cloudflare Images upload |
| `/api/index` | POST/DELETE | Embed + upsert content into Vectorize / delete vectors |
| `/api/clerk-webhook` | POST | Clerk auth webhook handler |
| `/api/settings` | GET/POST | Admin-gated. Read/write notification recipients + allowlist extras. Stored at `__settings:notifications` and `__settings:allowlist` |
| `/api/settings/allowlist-public` | GET | **Unauthenticated**. Returns merged admin allowlist (baseline + KV extras). Read by `assets/admin-gate.js` on every admin page load |
| `/api/health` | GET | Admin-gated. Live status of KV / Resend / Vectorize / Workers AI / Anthropic / Clerk / cron last-run |

### KV Key Conventions
```
{slug}:property        → full property JSON
{slug}:itinerary       → itinerary JSON
{slug}:source:{n}      → cached source text (slots 1-10)
{slug}                 → dossier text (plain string)
__property_index       → JSON array of all slugs
__index_cruises        → JSON {slugs:[...]} curated cruise order
__index_hotels         → JSON {slugs:[...]} curated hotel order
__index_villas         → JSON {slugs:[...]} curated villa order
member:{id}            → member JSON
__members_index        → JSON array of member IDs
__settings:notifications → JSON {recipients:[email], updatedAt} — admin alert recipients
__settings:allowlist   → JSON {emails:[email], updatedAt} — extra admin sign-in addresses
__cron:last_run        → JSON {ranAt, durationMs, scanned, sent, ok, error?} — set by stale-conv cron each run
```

---

## Site Structure

### Public Pages (35 total)
| Page | Purpose |
|------|---------|
| `index.html` | Homepage |
| `the-bearing.html` | About |
| `cruises.html` | Cruise listing — 1 hardcoded card (Nour El Nil) + KV dynamic loader |
| `hotels.html` | Hotel listing — hardcoded cards + KV dynamic loader |
| `villas.html` | Villa listing — hardcoded cards + KV dynamic loader |
| `property.html` | **Universal property template** — hydrates from KV via `?slug=` param |
| `nour-el-nil.html` | Nour El Nil flagship page (standalone, full content baked in) |
| `vibe-search.html` | AI-powered vibe search |
| `the-envoy.html` | Envoy AI landing page |
| `bookings.html` | Booking management |
| `my-account.html` | User account |
| `search.html` | Search |
| ~12 demo placeholder pages | Not real — ignore, will be replaced by `property.html?slug=X` |

### Admin Pages
| Page | Purpose |
|------|---------|
| `admin-properties.html` | Property list + **Listing Order tab** (drag to reorder) |
| `admin-property-editor.html` | Full property editor (name, type, location, price, images, rooms, dossier, sources, itinerary) |
| `admin-dashboard.html` | Overview dashboard |
| `admin-guests.html` | Guest management |
| `admin-bookings.html` | Booking management |
| `admin-conversations.html` | Envoy conversation history |
| `admin-analytics.html` | Analytics |
| + 8 more admin pages | Standard CRUD |

### Partner Portal Pages (`pp-*.html`)
Standard partner-facing pages: dashboard, listings, bookings, photos, rooms, availability, etc.

---

## Property System

### How Properties Work
1. Created/edited in `admin-property-editor.html`
2. Saved to KV as `{slug}:property` JSON + indexed in `__property_index`
3. Public URL: `https://thebearing.io/property?slug={slug}`
4. `property.html` fetches KV on load and hydrates all sections

### Property JSON Shape (key fields)
```json
{
  "name": "Gypsy by Mekong Kingdoms",
  "type": "River Cruise",
  "location": "Mekong River, SE Asia",
  "price_from": 2900,
  "tagline": "...",
  "bearing_edit": "...",
  "photos": { "hero": [{"url": "..."}] },
  "rooms": [...],
  "included": [...],
  "tags": [...],
  "render": { "location_line": "Mekong River · SE Asia" }
}
```

### Listing Page Logic (cruises/hotels/villas)
1. Fetch `__index_{type}` — curated admin-set order
2. If curated list exists: render those slugs in that order (type filter still applied)
3. Append any remaining type-matched KV properties not yet shown
4. Hardcoded flagship cards always sit at top (e.g. Nour El Nil on cruises page)

---

## Envoy AI System

### Architecture

| Layer | Status | Description |
|-------|--------|-------------|
| L1 | ✅ Done | Page-aware system prompts with per-property context |
| L2 | ✅ Done | Dossier authoring tool in admin (interview-based) |
| L3 | ✅ Done | AI-assisted dossier draft generation with web search |
| L4 | ✅ Done | RAG via Cloudflare Vectorize — index created, bindings added, worker updated |

## Auth System (Clerk)
- Clerk test key: pk_test_bWVhc3VyZWQtam9leS0xNS5jbGVyay5hY2NvdW50cy5kZXYk
- Clerk domain: measured-joey-15.clerk.accounts.dev
- Shared auth helper: assets/auth.js
- tbAuth.requireUser(cb, opts) — checks Clerk, shows slide-up sign-in modal if not authed
- tbAuthEnquire(propertyName) — auth-gated enquiry opener
- tbAuthBook(config) — auth-gated booking opener
- All enquiry + book buttons on property pages gated

### Admin allowlist (as of v72g)
- **Baseline:** `admin@thebearing.io` — hardcoded in `functions/api/envoy.js` (`ADMIN_EMAILS_BASELINE`) and `assets/admin-gate.js`. Never removable through the UI; it's the failsafe so a bad settings edit can't lock everyone out.
- **Extras:** Stored in KV at `__settings:allowlist`. Edited from `admin-settings.html`. Read by the worker via `loadAllowlistExtras()` and by `admin-gate.js` via `/api/settings/allowlist-public` on every admin page load (3s cap, baseline-only on fetch failure).
- To add a new admin: sign in to admin-settings.html → section 2 → add email → save. Takes effect on that admin's next page load (no redeploy)


## Booking System
- /api/booking — POST saves to KV, GET lists all, PATCH updates status
- KV key: booking:{ref}, index: __bookings_index
- Email via Resend (RESEND_API_KEY secret in worker)
- admin-bookings.html — real KV data, filter/confirm/cancel
- Pricing: roomPrice is per-trip flat rate
- **Recipient emails** for all operational admin alerts (booking, enquiry, conv reply, inbound email, 48h/72h stale escalation) are configurable from `admin-settings.html` and stored at `__settings:notifications`. `admin@thebearing.io` is always merged in as a baseline failsafe.

## Queued / Deferred (as of v72g)
- **admin-analytics wiring** — deferred until there's real traffic to chart. Page exists at `admin-analytics.html` but is a stub.
- **admin-payments (Stripe integration)** — deferred until there are real bookings to take payment for. Page exists at `admin-payments.html`.
- **L5 / L6 of Envoy roadmap** — global cross-property RAG index + persistent per-user memory. See section below.

| L5 | 🔲 Pending | Cross-property index for compare/recommend queries |
| L6 | 🔲 Pending | Persistent per-user conversational memory |

### L4 Vectorize Setup — BLOCKED
Cloudflare dashboard has no UI to create Vectorize indexes. Requires REST API call:

```bash
curl -X POST "https://api.cloudflare.com/client/v4/accounts/d62dd7db798247bb6cc9ff18ff7ee84f/vectorize/v2/indexes" \
  -H "Authorization: Bearer {CF_TOKEN_WITH_VECTORIZE_EDIT}" \
  -H "Content-Type: application/json" \
  -d '{"name":"thebearing-properties","config":{"dimensions":768,"metric":"cosine"}}'
```

Need a CF API token with `Account → Vectorize → Edit` permission.
After index is created:
1. Add `VECTORIZE` binding in Pages → Settings → Functions
2. Add `AI` binding in Pages → Settings → Functions
3. Update `wrangler.toml` with both bindings
4. Update worker with `/api/embed`, `/api/vectorize` endpoints + upgrade `/api/envoy` to query vectors first

### Envoy Client Code
- Shared prompt: `assets/envoy-prompt.js`
- Each page has a `ci-drawer` FAB that opens the Envoy chat
- Property pages inject dossier + property data into system prompt
- Context pill shows current property name
- Suggestion chips hidden when conversation history exists

---

## Key Design Decisions & Gotchas

### Deployment
- **Always push to GitHub** — never `wrangler deploy` locally (Mac env causes crashes)
- File structure is flat — all HTML files in root, assets in `/assets`, functions in `/functions/api/`

### Mobile Overlays
- All `.sheet` overlays slide up from bottom on mobile (`≤767px`)
- `translateY(100%) → translateY(0)` CSS transition
- `tbOpenCheckout()` uses double `requestAnimationFrame` + `.open` class
- `tbCloseCheckout()` removes `.open`, waits 380ms before `display:none`
- X close button on all sheets, tap outside backdrop also closes

### Book Bar (all property pages)
- Dual buttons: **Book now** (triggers `.dbc-cta`) + **Enquire** (opens `inq-overlay`)
- Both on `property.html` AND `nour-el-nil.html` AND all 12 demo pages
- Mobile: `flex:1` so both share equal width

### Safari Backdrop-Filter Bug
- `backdrop-filter` on `display:none` elements still renders blur in Safari
- Fix: add `visibility:hidden` to `.overlay` + `visibility:visible` to `.overlay.open`
- Applied to all 12 old-template pages

### Listing Order
- Admin: `admin-properties.html` → Listing Order tab → drag to reorder → Save
- Saves `__index_cruises`, `__index_hotels`, `__index_villas` to KV
- `hasCurated` must be declared at IIFE outer scope — chained `.then()` callbacks don't share scope

### Image Handling
- All images via Cloudflare Images
- Delivery: `https://imagedelivery.net/YyCqpmHo4EG6ShyDMCRcVQ/{id}/public`
- No `nourelnil.com` images on any page except intentionally on `nour-el-nil.html`

### CSS Injection Rules
- Never inject CSS outside `<style>` tags (causes wall-of-code rendering)
- Always close `</style>` before opening new `<style>` block
- One FAB (`ci-fab`) per page maximum

---

## Current Real Properties in KV
| Property | Slug | Type | Page |
|----------|------|------|------|
| Nour El Nil | `nour-el-nil` | River Cruise | Standalone `nour-el-nil.html` |
| Gypsy by Mekong Kingdoms | `gypsy-by-mekong-kingdoms` | River Cruise | `property.html?slug=gypsy-by-mekong-kingdoms` |
| Singita Grumeti | `singita-grumeti-x` | Lodge | `property.html?slug=singita-grumeti-x` |

---

## Nour El Nil Mobile App (separate project)
- Repo: `mcancino213/nour-el-nil-app-repository`
- **Build via Expo dashboard only** — never local CLI
- Branch: `main`, profile: `testflight`
- Stable build: 1.0.0 (13), SDK 54
- Audio: `react-native-sound`, `.default` unwrap fix in `phraseAudio.ts`
- Hidden debug: 5-tap on "NOUR EL NIL" header → AudioDebugScreen (remove before ship)
- Open items: progress persistence, pulse/review POST endpoint, Day 6 content audit, meditation audio (friend recording), push notifications, Envoy AI integration

---

## Wrangler Config (`wrangler.toml`)
```toml
name = "thebearing-io"
main = "functions/api/envoy.js"
compatibility_date = "2024-09-01"

[assets]
directory = "./"
not_found_handling = "single-page-application"

[observability]
enabled = true

[[kv_namespaces]]
binding = "DOSSIERS"
id = "aa0c885871474266966e50f0676dd019"

# TO ADD after Vectorize index is created:
# [[vectorize]]
# binding = "VECTORIZE"
# index_name = "thebearing-properties"
#
# [ai]
# binding = "AI"
```

---

## Build History (recent)
| Build | Key changes |
|-------|-------------|
| v75l | **pp-team built (schema option C) + new `/api/partner-team` endpoint.** First of the schema-design-first trio (pp-team / pp-availability / pp-experiences). pp-team manages the property's `partnerUserIds` — the same array the whole portal uses for authorization — as a real "who has access" UI. **New endpoint `/api/partner-team`:** same auth model as partner-media/partner-listing (Clerk-session-verified identity + partnerUserIds membership; admins always allowed). GET `?slug=X` → members list with names/emails resolved from Clerk (`/v1/users/{id}` via CLERK_SECRET_KEY, best-effort — falls back to showing the bare id if Clerk lookup fails). POST `{slug, action:'add'|'remove', userId}` → mutates partnerUserIds. **Guards:** add requires a `user_`-prefixed id; remove refuses to empty the list (can't orphan the property with zero partner access). **pp-team.html:** member cards (avatar initials, name, email, "You" badge), remove buttons (disabled when only one member remains), and an add-by-user-ID field with explainer that the person needs an existing Bearing account and that email invites are coming later. DOM-methods only. Defaults PP_SLUG to nour-el-nil-x with `?as=` override. **SECURITY NOTE (also in code comment):** letting a partner add arbitrary Clerk user IDs to their own property is a privilege-escalation vector IF untrusted partners ever get portal access — a partner could add an accomplice. Acceptable while the operator is sole/trusted (current state). If untrusted partners are onboarded, gate 'add' behind admin approval or an invite flow. Remove is always safe. Email-invite flow is the deferred follow-up. **Verify after deploy:** (1) /pp-team lists current members — should show Miguel (with "You" badge), name/email resolved from Clerk. (2) Add a test: paste another known user_ id → appears in list. (3) Remove it → gone. (4) Try to remove the last member → blocked with "cannot remove the last team member". (5) Cross-check: /admin-property-editor?slug=nour-el-nil-x section 0 Partner user IDs reflects the same list (same underlying field). **Remaining in trio:** v75m = pp-availability (manual blackout calendar, channel-manager/iCal later — per Miguel), v75n = pp-experiences (full stack: add experiences section to property.html matching the nour-el-nil design reference, add admin editor section, then partner editor). |
| v75k | **pp-photos + pp-rooms built (both in one build) + new `/api/partner-media` endpoint + `/api/upload` auth gate.** Both pages were "Get started" stubs; both needed backend work because `photos` and `rooms` are structured arrays deliberately excluded from the partner-listing whitelist (v75b). **New endpoint `/api/partner-media`:** same auth model as partner-listing (Clerk-session-verified identity + partnerUserIds membership; admins always allowed). GET `?slug=X` → `{ok, slug, photos, rooms}`. POST `{slug, photos?, rooms?}` → read-modify-write updating only the provided field(s), leaving the rest of the property record untouched. Server-side sanitizers coerce input into the canonical shapes. **Canonical shapes — VERIFIED to match admin-property-editor's updateJSON output exactly** (critical, since property.html renders both admin- and partner-written records): photos = `{hero:[{url,position}], gallery:[{url,position}]}`; rooms = `[{name, type, price(int), per, size(int|null), size_unit, beds, max_occupancy(int|null), description, photos:[{url,position}]}]`. Diffed all 10 room fields + photo shape against admin editor lines 2476-2522 — identical. Nameless rooms are dropped (matches admin behavior). **`/api/upload` gated:** was completely open (anyone could POST images and run up the Cloudflare Images bill). Now requires an admin session OR any verified Clerk session. The upload isn't property-scoped (the image isn't attached to a record until saved via partner-media, which IS property-scoped) — requiring any verified session is enough to stop anonymous abuse. Only caller was admin-property-editor (loads admin-fetch → isAdmin passes), so no existing flow breaks; new pp-photos/pp-rooms upload via partner-fetch (JWT) → getRequesterUserId passes. **pp-photos.html:** hero (max 8) + gallery (max 12) sections. Per-image: upload (multi-file), delete, reorder (↑/↓), position select (center/top/bottom/left/right). First hero image badged "Primary". Save button arms on change; beforeunload guard. **pp-rooms.html:** card-per-room with all 10 fields (name, type, price, per-select, size+unit, beds, max guests, description textarea) + per-room photo upload (max 6, delete). Add/remove/reorder rooms. On save, re-syncs state from the server's cleaned response so the editor matches stored exactly. Both use DOM methods only (createElement/textContent/addEventListener), no innerHTML string-concat with inline onclick (per project rule). Both default PP_SLUG to nour-el-nil-x with `?as=` override (consistent with v75h). **Verify after deploy:** (1) /pp-photos loads existing photos (if any) for nour-el-nil-x; upload a test image → appears in grid; reorder/position/delete work; Save → "✓ Saved"; refresh → persists. (2) /pp-rooms loads existing rooms; add a room, fill fields, upload a room photo; Save → persists; refresh shows it. (3) Cross-check: after saving from the partner side, open /admin-property-editor?slug=nour-el-nil-x → the same photos/rooms appear (proves shape compatibility). (4) Anonymous abuse check: signed out, `fetch('/api/upload',{method:'POST',body:new FormData()})` → 403. (5) Admin photo upload in admin-property-editor still works (gate allows admin). **Scope note:** Miguel chose to build both pages in one build (vs the staged split Claude recommended). Shapes were carefully verified to mitigate the bundling risk. |
| v75j | **pp-booking-detail.html built — real read-only booking view (was a "Get started" stub).** This is the destination that pp-dashboard's booking "View" links already pointed at (`pp-booking-detail?ref=X`) but which rendered a placeholder. Now it fetches `/api/booking?ref=X` (partner-fetch.js attaches the Clerk JWT; the v75i `?ref=` auth lock authorizes admin / partner-of-property / booking-guest) and renders: header with ref + guest name + email + status badge; three fact cards (Stay: dates/nights/room/guests, Value: total/deposit/payment status, Property: name/enquiry date); the guest's enquiry notes (if any); offer history (if any offers exist, newest first with amount/date/status badge); and contextual links to the conversation (resolves the booking's `conversationId` when present, else falls back to the bare pp-conversations page). Honest error states for: no ?ref= param, 403 (not authorized), booking not found, fetch failure. **Scope decision — deliberately read-only:** pp-bookings.html already has richer in-page modals for acting on bookings (openConfirmedDetailModal, openOfferModal, openAmendmentModal for build/revise offer + amend). pp-booking-detail does NOT duplicate those — it's the standalone, bookmarkable/shareable single-booking URL (the dashboard link target), with a footer note directing partners to pp-bookings or the conversation for actions. pp-bookings was intentionally NOT changed to link here — that would downgrade its modal flow to a read-only page. **Verify after deploy:** (1) from /pp-dashboard, click "View" on a booking in the upcoming/recent list → lands on pp-booking-detail with that booking's real data. (2) Direct URL /pp-booking-detail?ref=TB-XXXX-XXXX loads the booking. (3) /pp-booking-detail with no ?ref= shows the "No booking specified" error. (4) The offer-history card appears only when the booking has offers. (5) "View conversation" link appears and routes to the right thread. **Note:** booking fields rendered defensively — confirmed_total_amount falls back to totalAmount/total_amount, deposit and payment-status similarly, so both enquiry-stage and confirmed bookings render sensibly. |
| v75i | **Customer-endpoint data-layer security — closes the last open authorization hole.** This is the matching half of the partner security work (v75f/v75g). Before this, any signed-in user could read another guest's bookings or conversations by guessing/knowing a booking ref, email, conversation id, or Clerk user id — the customer-scoped endpoint branches were trust-on-URL. v75i locks all four. **The four branches now enforced:** (1) `/api/booking?ref=X` → admin OR partner of the booking's property OR the booking's guest (matched by Clerk user id OR a verified owned email). (2) `/api/booking?email=X` → admin OR a signed-in user who actually owns email X (verified against their Clerk user record, not the URL). (3) `/api/conversation?id=Y` → admin OR partner of the conversation's property OR the conversation's guest. (4) `/api/conversation?guestId=X` → admin OR a signed-in user whose verified id matches X. **New worker helper:** `getRequesterEmails()` — fetches the verified requester's Clerk email addresses (cached per request) so `?email=` lookups can confirm the requester owns the email rather than trusting the URL param. Uses the existing JWT-verified `getRequesterUserId()` from v75g. **New client asset:** `assets/customer-fetch.js` — wraps `window.fetch` to attach the Clerk session JWT (via `Clerk.session.getToken()`) on same-origin `/api/*` calls, mirroring partner-fetch.js and admin-fetch.js. Critically, it's **safe on public pages**: it only adds the header when a Clerk session exists, and it skips the readiness wait entirely if `window.Clerk` isn't present (property.html and nour-el-nil.html load Clerk lazily via auth.js only on sign-in interaction — so anonymous browsing never stalls). Added to the 4 customer pages that call these endpoints: bookings.html, conversations.html, nour-el-nil.html, property.html. **Why this is safe for existing flows:** every customer call to these endpoints already only fires when a user is signed in (e.g. conversations.html sets `guestId = user.id` inside a signed-in block; property.html's `?email=` check only runs when `clerkEmailForCheck` is set). The verified token's `sub` equals the stored `guestId` by construction, so legitimate guests pass; impersonation attempts (wrong id/email) get 403. **Known consequence — debug utility pages:** `list-convs.html` (raw JSON dump of all conversations) calls bare `/api/conversation`, which has been admin-only since v75f. It has no auth wrapper, so it now returns 403 unless opened by an admin session. These are leftover developer debug tools, not customer flows — left as-is (a 403 on an unauthenticated raw-dump tool is arguably correct). `archive-conv.html`/`unarchive-conv.html` use PATCH (not touched by this build). **Security status after v75i:** all read endpoints that return personal/partner data now verify identity server-side via Clerk JWT. The gate (v75e) stops UI access; the data layer (v75f/g/i) stops API access. **Verify after deploy:** (1) Customer: sign in, open /bookings.html — your bookings still load. Open /conversations.html — your conversations still load. (2) Cross-account test: signed in as customer A, run in console `fetch('/api/conversation?guestId=SOME_OTHER_USER_ID').then(r=>r.json()).then(console.log)` → should 403 (guestid_mismatch). (3) `fetch('/api/booking?email=someone-elses@email.com').then(r=>r.json()).then(console.log)` → 403 (email_not_owned). (4) Partner flows (pp-*) still work. (5) Admin flows still work. (6) Public: open /property?slug=... or /nour-el-nil signed OUT — page loads normally, no stall (customer-fetch skips the wait when Clerk absent). |
| v75h | **Pointed the partner portal at `nour-el-nil-x` (the editor-built record with real data) instead of `nour-el-nil`.** Resolves the 403s that surfaced when picking the project back up. **Root cause (diagnosed live, not guessed):** the partner portal's `PP_SLUG` defaulted to `nour-el-nil`, but that slug had NO KV property record — the public Nour El Nil page is hardcoded HTML (`nour-el-nil.html`), never created through the editor, so no `nour-el-nil:property` record ever existed. The v75f/v75g auth check (`isPartnerOf(slug, userId)` reads the property record's `partnerUserIds`) therefore found nothing and returned 403 on every partner data call. **The actual data** (bookings, enquiries) lives under a SEPARATE editor-built record, `nour-el-nil-x`, which was created through the normal editor flow. **Design intent confirmed with Miguel:** the hardcoded `nour-el-nil` page is the *design source of truth* — a reference Miguel uses to make sure editor-built pages match the original design. `nour-el-nil-x` is the operational record the partner portal manages. These are deliberately kept separate and NOT merged: the hardcoded page controls public display, the `-x` record holds operations/data. pp-listing edits save to `-x` (partner's own record-keeping) and intentionally do NOT change the hardcoded public page. **Changes:** `PP_SLUG` default changed from `nour-el-nil` → `nour-el-nil-x` in 6 partner pages (pp-analytics, pp-bookings, pp-conversations, pp-dashboard, pp-listing, pp-notifications) plus `assets/pp-sidebar-badges.js` (unread badge counts). The `?as=X` override param is preserved on all. Preview-listing links unchanged — the ternary already resolves `nour-el-nil-x` → `/property?slug=nour-el-nil-x` (the partner's own rendered page). **Manual data steps performed via console (not in this zip, already live):** (1) deleted the empty stray `nour-el-nil:property` record that had been created earlier in the session via a wrong assumption; (2) added Miguel's Clerk user ID `user_3CARwvbpWkTQOJmPA8q4LAOrp8f` to the `nour-el-nil-x` record's `partnerUserIds` via admin read-modify-write. **Note on the abandoned approach:** earlier this session, before the slug situation was understood, a `nour-el-nil` KV record was created and the v75 chain (v75-v75g) assumed the portal slug was `nour-el-nil`. That record was deleted; this build corrects the slug across the portal. **Verify after deploy:** (1) /pp-dashboard loads with `nour-el-nil-x`'s real data — enquiries/bookings counts should now reflect the actual history (the "Nour El Nil X" enquiries seen in the admin activity feed), not zeros. (2) No 403s in console. (3) pp-bookings, pp-conversations show the real records. (4) `?as=` override still works for testing other partners. (5) admin side unchanged. **Stale comments left as-is:** pp-bookings.html lines ~650 and ~1947 have comments referencing the old `nour-el-nil` default — harmless, not functional code, left to avoid risk. |
| v75g | **Fixed v75f's broken session verification — partner data fetches and pp-listing save work again.** v75f attempted data-layer auth but the server-side identity check was built on a wrong assumption about Clerk's API: it treated the `X-Clerk-Session` header value as a session ID and tried to look it up via `GET https://api.clerk.com/v1/sessions/{id}`, which doesn't verify that way. Every call returned null → every partner data fetch 403'd → pp-listing save broke. (This is the exact "stop guessing when something should work" lesson from the admin-gate saga — the admin code even documents that this Clerk REST path doesn't work, and I repeated the mistake.) **The correct approach (verified against Clerk's current docs before coding this time):** Clerk session tokens are RS256-signed JWTs. The client sends the JWT (from `Clerk.session.getToken()`), and the worker verifies the JWT signature against Clerk's public keys, then reads the user id from the `sub` claim. **Worker changes:** (a) New module-level `verifyClerkJWT(token, env)` helper — decodes the JWT, validates `exp`/`nbf`, fetches Clerk's JWKS from `{iss}/.well-known/jwks.json` (derived from the token's issuer claim), imports the RSA public keys via Web Crypto `crypto.subtle.importKey`, verifies the signature via `crypto.subtle.verify`. JWKS keys cached at module scope (persist across requests in a worker instance); on a verify failure it re-fetches once to handle key rotation. Networkless after the first JWKS fetch. (b) `getRequesterUserId()` rewritten to call `verifyClerkJWT` and return the `sub` claim. (c) No new env secret required — JWKS is fetched from Clerk's public well-known endpoint. **Client changes:** `assets/partner-fetch.js` now sends the JWT from `await Clerk.session.getToken()` instead of `Clerk.session.id`. This matches what admin-fetch.js already does (admin-fetch was already correct — it sends getToken()). **Why admin auth is unaffected:** `isAdmin()` is unchanged and uses the email-header path; `getRequesterUserId()` is only called in partner/guest authorization paths. Admin requests carry the email header AND now a verifiable JWT, but isAdmin() returns true via email first, so partner checks are skipped. **Scope unchanged from v75f:** only `?slug=X` partner queries are locked (`/api/booking?slug=`, `/api/conversation?slug=`, `/api/partner-listing`). Customer queries (`?ref=`, `?email=`, `?id=`, `?guestId=`) still open with TODO v75g→v75h markers (renamed; customer-fetch.js hardening still pending). **Unresolved from v75f testing:** Miguel saw a 404 on partner-listing GET (separate from the 403 auth issue). Likely the slug param wasn't reaching the endpoint, or was being tested as admin where acl passes but KV lookup missed. Should re-test after v75g; if the 404 persists, capture the FULL request URL (the screenshot truncated the slug) to diagnose. **Verify after deploy:** (1) sign in as partner. (2) /pp-dashboard loads with Nour El Nil data (no 403s in console). (3) pp-listing loads real data, edit a field, Save → "✓ Saved", refresh → persists. (4) DevTools console: `fetch('/api/booking?slug=nour-el-nil').then(r=>r.json()).then(console.log)` → returns bookings. (5) `fetch('/api/booking?slug=gypsy-by-mekong-kingdoms').then(r=>r.json()).then(console.log)` → 403 (not your property). (6) Customer + admin flows still work (regression check). |
| v75f | **Data-layer authorization for partner-scoped API calls — closes the v75e gap where signed-in users could fetch ANY partner's data via direct API calls.** v75e added the auth gate that stops signed-out browsers from rendering partner pages, but the underlying API endpoints had no per-user authorization — anyone with a valid Clerk session (any signed-in user) could call `/api/booking?slug=any-property` and get that property's bookings. v75f locks partner-scoped query branches to require the requester be either an admin OR a partner of that specific property. **Scope decision: only partner-scoped queries (`?slug=X`) are locked in v75f.** Customer-facing query branches (`?ref=`, `?email=`, `?id=`, `?guestId=`) remain open with TODO v75g markers, because hardening them would break customer flows (bookings.html, conversations.html, my-account.html, nour-el-nil.html, property.html, list-convs.html, unarchive-conv.html) that don't yet use a Clerk-session fetch wrapper. v75g will ship customer-fetch.js + flip the remaining locks. **Worker changes:** (a) Three new helpers added after `adminDenied()` in functions/api/envoy.js: `getRequesterUserId()` verifies the `X-Clerk-Session` header by calling `https://api.clerk.com/v1/sessions/{sid}` and returns the verified user_id (closure-cached per request to avoid double-verification); `isPartnerOf(slug, userId)` reads the property record and checks the `partnerUserIds` array; `partnerDenied(reason)` is a uniform 403 response shape. (b) `/api/booking` GET hardened: `?slug=X` now requires admin OR partner of X (returns 403 with reason); bare list (no params) is admin-only. `?ref=` and `?email=` remain open with TODO v75g. (c) `/api/conversation` GET hardened: `?slug=X` requires admin OR partner of X; bare list is admin-only. `?id=` and `?guestId=` remain open with TODO v75g. (d) `/api/partner-listing` hardened: the v75b version trusted a `userId` URL/body param. v75f verifies the requester via Clerk session token and uses THAT as the authorization subject. The URL param is still accepted for backwards compat but only used for diagnostic logging; if the URL claim and the session disagree, a warning is logged and the session wins. **Client changes:** (a) New `assets/partner-fetch.js` — wraps `window.fetch` to attach `X-Clerk-Session` header on same-origin `/api/*` calls. Awaits Clerk readiness (cap 8s) before forwarding the request, so page-load fetches don't race ahead of Clerk loading. Idempotent install via `window.__tbPartnerFetchInstalled`. (b) partner-fetch.js added to all 13 partner pages right after partner-gate.js. **Trust model after v75f:** signed-out users see nothing (v75e gate). Signed-in non-partners cannot fetch any property's data — `/api/booking?slug=X` returns 403 unless they're in X's partnerUserIds. Signed-in partners only see their own property's data. Admins still see everything (admin-fetch.js + isAdmin() path unchanged). **What's NOT secured yet (v75g work):** any signed-in user can still fetch `/api/booking?ref=any-booking-ref`, `/api/booking?email=anyone@anywhere`, `/api/conversation?id=any-conv-id`, `/api/conversation?guestId=any-user-id`. These need customer-fetch.js + per-query checks. **Verify after deploy:** (1) sign in as your partner account. Navigate to /pp-dashboard — should load normally (bookings/conversations data appears for Nour El Nil). (2) Open DevTools Console. Run `fetch('/api/booking?slug=nour-el-nil').then(r=>r.json()).then(console.log)` — should return your bookings (your partner-fetch.js wrapper attaches the header). (3) Run `fetch('/api/booking?slug=some-other-property').then(r=>r.json()).then(console.log)` — should return 403 with "not_partner_of_some-other-property". (4) Run `fetch('/api/booking?slug=nour-el-nil', {headers:{'X-Clerk-Session':''}})` to bypass the wrapper — should also return 403 (worker server-side check, not just client-side). (5) Test pp-listing save still works (the v75b flow). (6) Test admin-bookings / admin-conversations still load all properties (admin-fetch.js path). |
| v75e | **Real partner auth gate — closes the security gap finally exposed by working sign-out.** Until v75c, sign-out didn't actually sign out (Clerk session lived on, just local cache cleared), so the lack-of-auth-gate problem was hidden. After v75c made sign-out real, Miguel verified that a signed-out user could still navigate to /pp-dashboard and see Nour El Nil's bookings — the partner pages were rendering without any auth check. **What this build adds:** (a) New asset `assets/partner-gate.js` — mirrors admin-gate.js. On every partner page load: hides body via inline `<style>`, waits up to 15s for Clerk to fully load (`Clerk.loaded === true`), checks `Clerk.user`, reveals body if present OR redirects to /pp-login if absent. Also refreshes `tb_pp_user_cache` with current Clerk data on each load (centralizes cache-writing — previously only pp-login wrote it). Watches for sign-out via `Clerk.addListener` and redirects immediately if user becomes null in another tab. (b) Clerk SDK + partner-gate.js added to all 12 unguarded partner pages: pp-analytics, pp-availability, pp-booking-detail, pp-bookings, pp-conversations, pp-dashboard, pp-experiences, pp-listing, pp-notifications, pp-photos, pp-rooms, pp-team. pp-settings already had Clerk (v75c) so only the gate was added. pp-login is intentionally NOT gated (would cause infinite redirect loop). (c) First-load body-hide skipped on subsequent navigations in the same session (`tb_pp_verified_v1` flag in sessionStorage). Mirrors the admin-gate trick that lets view transitions animate between partner pages instead of white-flashing. If session was revoked between navs, the in-memory Clerk check still catches it and redirects. (d) Side fix — the v74u VT-opt-in head script on every partner page was reading from the stale cache key `tb_pp_sidebar_v1`. SIDEBAR_VERSION has been at 3 since v75c, so the pre-warm never fired. All 13 partner pages now read from `tb_pp_sidebar_v3`. (e) pp-listing's `load()` extended with `waitForUserId(8000)` — the gate writes the cache after Clerk loads (200-500ms), but pp-listing's initial load fires on script execution which can race ahead of the gate. The wait+poll resolves once the cache appears or after 8s gives up (at which point the gate would have already redirected anyway if Clerk failed). **Trust model — what this DOES and DOES NOT do**: the gate stops signed-out browsers from seeing partner page chrome and rendering data. It does NOT prevent a determined attacker who disables JS or strips the gate script — the data endpoints (`/api/booking?slug=`, `/api/conversation?slug=`) currently return data without user-level authorization. **That's the v75f hardening pass:** add Clerk session validation to those endpoints AND check the requesting user is in the property's `partnerUserIds` list. The gate is honest UX protection, not real security yet. **Verify after deploy:** (1) sign out completely (sidebar footer → pp-settings → Sign out). (2) Manually navigate to /pp-dashboard — should redirect to /pp-login (URL changes to /pp-login). (3) Try /pp-bookings, /pp-conversations, etc. directly — all should redirect. (4) Sign in via /pp-login — lands on /pp-dashboard, header reads "Welcome back, [name]", sidebar footer shows your avatar/name. (5) Navigate around the portal — every page loads, no flash, no redirect. (6) In another tab, sign out — within seconds the original tab should also kick to /pp-login (Clerk.addListener watching). |
| v75d | **pp-login redirect fix — signing in via Google/Apple OAuth no longer lands the partner on the homepage with a `__clerk_handshake=…` URL parameter.** Real bug found during v75c verification: Miguel signed in via pp-login, expected to land on `/pp-dashboard`, instead got `https://thebearing.io/?__clerk_handshake=…`. **Root cause:** Clerk 6.x deprecated the `afterSignInUrl` mountSignIn option (which is what pp-login was using). It still works for inline-form sign-ins, but for OAuth redirects (Google, Apple, etc.) the redirect URL is configured in the Clerk dashboard, not at the client call site — and the dashboard default is `/`. The new options are `signInForceRedirectUrl` (always redirect there) and `signInFallbackRedirectUrl` (redirect there when Clerk has no other intent). The pp-login `addListener(...)` callback only fires for inline-form sign-ins, not for OAuth roundtrip flows, so even the existing `window.location.href = '/pp-dashboard'` line in the listener wasn't catching OAuth completion. **Two fixes shipped together:** (a) pp-login mountSignIn now passes all three redirect-URL variants (`afterSignInUrl`, `signInForceRedirectUrl`, `signInFallbackRedirectUrl`) all set to `/pp-dashboard`. This handles the case where Clerk 6 actually honors the call-site option. (b) Belt-and-suspenders: pp-login writes a `tb_signin_intent` localStorage entry on page load (`{target: '/pp-dashboard', setAt: timestamp}`) so that IF the OAuth flow still lands the user on the homepage with `__clerk_handshake=…` in the URL (because the OAuth redirect URL is dashboard-configured and may not honor the client option), a new bootstrap script on index.html catches it: it checks for `__clerk_handshake` in `location.search` AND a fresh (<10 min) sign-in intent in localStorage, consumes the intent (so a reload doesn't redirect again), waits 800ms for Clerk to consume the handshake parameter and establish the session, then redirects to the intent target. localStorage rather than sessionStorage because OAuth redirects through google.com / apple.com can lose sessionStorage in some browser modes. Intent expires after 10 minutes to prevent stale intents from ricocheting users forever. **Same fix applied to: pp-login only.** admin-login and customer my-account/bookings sign-ins have the same `afterSignInUrl` deprecated pattern, but the index.html intent handler is specific to partner — if customers hit the same OAuth bug their fix needs separate intent values. Flagged as a followup task. **Verify after deploy:** (1) sign out completely from current session (use sidebar footer → pp-settings → Sign out — v75c fix). (2) navigate to /pp-login. (3) sign in via Google or Apple. (4) Should now land on /pp-dashboard, NOT on the homepage. If you DO land on the homepage with `__clerk_handshake=…` in the URL, watch — within 1 second the belt-and-suspenders script should redirect you to /pp-dashboard. Either way the destination should be correct. (5) check DevTools Application → Local Storage → `tb_signin_intent` should be absent after successful sign-in (consumed by the rescue script). |
| v75c | **Sign-out actually signs out now (was a real bug in v75b).** Root cause: pp-settings.html had a sign-out button whose handler checked `if (window.Clerk && typeof window.Clerk.signOut === 'function')` before calling Clerk.signOut() — but pp-settings doesn't load the Clerk SDK, so `window.Clerk` was always undefined, the if-block never executed, the Clerk session was never terminated. The local sessionStorage caches got cleared (so the UI appeared to sign out) but Clerk's server-side session lived on. Result: clicking Sign out → redirected to /pp-login → Clerk says "you're already signed in" → bounced back to /pp-dashboard. Looked like sign-out did nothing. This is the same fundamental gap flagged in v74x/v75 — partner pages don't load Clerk after pp-login — but for the sign-out operation specifically, you actually need Clerk loaded. So v75c adds the Clerk script tag to pp-settings (and only pp-settings — keeping the rest of the partner portal lightweight). **Implementation:** (a) added the `<script src="...clerk.browser.js" data-clerk-publishable-key="...">` tag to pp-settings.html. (b) sign-out handler rewritten as `waitForClerk(6000)` polling loop that waits for `window.Clerk.signOut` to become a function, then awaits Clerk.load() if needed, then calls signOut(), then clears local caches, then redirects. Failure path: if Clerk never loads (script blocked / network), shows alert "Sign-out is unavailable right now (Clerk failed to load)" and re-enables the button — doesn't silently no-op. **Also in v75c — sidebar footer now clickable**: previously the partner name+avatar in the sidebar footer was a plain `<div>`. Now it's an `<a href="pp-settings">` so clicking the user info takes the partner to settings (where they can sign out). Bumped SIDEBAR_VERSION from 2 to 3 to invalidate the v2 cache that had the unclickable footer. Sign-out cleanup updated to also clear `tb_pp_sidebar_v3`. **What this still does NOT solve:** the underlying lack-of-Clerk-on-partner-pages issue — partner pages other than pp-settings still can't access `window.Clerk.user` for things like real per-user identity checks, real auth gates, or "you've been signed out elsewhere" detection. The right long-term fix is loading Clerk lazily across the partner portal (or replacing the cache with a server-checked session). That's still the next architectural task. **Verify after deploy:** (1) sign in via /pp-login. (2) navigate to /pp-settings. (3) click Sign out — should briefly show "Signing out…" then redirect to /pp-login showing the sign-in form (NOT bounce back to /pp-dashboard). (4) check DevTools Application → Session Storage on /pp-login — `tb_pp_user_cache` should be absent. (5) navigate manually to /pp-dashboard — should bounce you to /pp-login because no session exists. |
| v75b | **pp-listing made real (was the worst kind of fake — looked editable but did nothing).** First page rebuilt in the multi-build campaign to make every partner-portal page genuinely functional. **The old pp-listing was a UI mockup:** every input had a hardcoded `value="Nour El Nil"` / `value="Egypt"` etc. — partners could spend 20 minutes editing fields, click away, and silently lose everything. The "Submit for review" button was decorative. **What v75b ships:** (1) New partner-gated endpoint `/api/partner-listing` GET + POST. GET returns ONLY the editable subset of the property record. POST accepts only a whitelist of 22 editable fields (name, tagline, type, country, region, nearest_airport, display_tag, short_pitch, long_pitch, long_history, price_from, price_currency, pricing_model, season_open, min_stay, max_group, checkin_time, checkout_time, children_policy, pet_policy, included, not_included, tags, amenities). Operational/admin fields (commission_pct, slug, status, partnerUserIds, partner_emails, photos, rooms) are NOT touchable through this endpoint — the whitelist is server-enforced. Anything else in the POST body is silently rejected with a `rejected:[]` array in the response. (2) New schema field on property records: `partnerUserIds` (array of Clerk user IDs permitted to use the partner editor for this property). Empty default. Backfilled on-read in `/api/property` GET. (3) Admin UI for partnerUserIds: new chip-list field in admin-property-editor under section 0 (Commercial), right below the existing partner_emails chip list. Pattern identical to partner_emails. Loads from `saved.partnerUserIds` on property load, serializes back on save. Light Clerk-id format validation (must start with `user_` and be ~32 chars). Help text explains how partners can find their own ID via `window.Clerk.user.id` in the console. (4) Rebuilt pp-listing.html as a real editor: same 4-section visual layout as before (Core identity / Description / Rates & availability / Need-to-know), but every input now has `data-field="X"` matching the worker's whitelist, populated on load from `/api/partner-listing?slug=X&userId=Y`. Trust model: reads userId from `tb_pp_user_cache` (written by pp-login from Clerk). Without a cached session → "You need to sign in" error state. With session but userId not in partnerUserIds → "Not authorized" error state. (5) Save model is explicit (not auto-save): tracks dirty fields, Save button shows count ("Save 3 changes"), Discard reverts to last-saved snapshot. Sends only the diff to the worker. Success → snapshot updates, dirty clears, "✓ Saved" status fades after 2.5s. Save errors → alert, button re-enables. (6) Live preview link wires to /nour-el-nil for that slug or /property?slug=X for others. (7) Card preview in right sidebar updates live as the partner types (real-time renders name / type / region / tagline / price). (8) Publish checklist replaced with real computation from current form values (7 items: name, type, hook, short description, country, starting price, what's included). (9) Removed dishonest UI elements: the old "Status: Live/Draft/Paused" select (admin-only field — partners can't change it); the "Submit for review" button copy (suggested an approval workflow that doesn't exist — replaced with "Save changes"); the "Review status: Changes will be reviewed within 24 hours" sidebar card (replaced with a "Listing controls" card explaining operational settings are admin-managed via partners@thebearing.io). **What this does NOT change:** photos, rooms, experiences, availability, team, booking-detail are all still stubs. Those land in v75c (photos/rooms/booking-detail) and a later build for experiences/availability/team (the last three need new schema design before they can be built honestly). **One-time setup required before partners can use this:** an admin must add the partner's Clerk user ID to the property's partnerUserIds list via admin-property-editor. To find Miguel's own Clerk user ID for testing: (a) navigate to /pp-login, (b) sign in, (c) open browser DevTools console and run `window.Clerk.user.id` — copy that, (d) go to /admin-property-editor?slug=nour-el-nil, (e) paste into the new "Partner user IDs" field and Save. Now pp-listing.html will load that partner's data and accept their edits. **Verify after deploy:** (1) without the partnerUserIds setup, /pp-listing should show "Not authorized" error. (2) After setting partnerUserIds in admin, /pp-listing should load with real Nour El Nil data populating every field. (3) Edit one field — Save button enables and shows "Save 1 change". (4) Click Save — should briefly show "Saving…" then "✓ Saved". (5) Refresh the page — the change should persist. (6) Open /admin-property-editor?slug=nour-el-nil — the same change should be visible there too (same KV record). |
| v75a | **pp-dashboard slug-fallback prettifier.** v75 verification surfaced a bug: when not signed in, the header read "Welcome back to nour-el-nil" (raw slug) instead of "Welcome back to Nour El Nil" (display name). Root cause: my v75 code used `property && property.name ? property.name : PP_SLUG` — and when either the `/api/property?slug=nour-el-nil` fetch failed OR the KV record had an empty/missing `name` field, the fallback was the raw slug verbatim. **Fix:** new `prettifySlug()` helper that converts kebab-case to Title Case (`"nour-el-nil"` → `"Nour El Nil"`). Used as the property-name fallback in `applyHeader()`. Intentionally dumb — if a property has a name with edge characters (acronyms, special chars, `&`), the real `name` field on the KV record should be set so this fallback never runs. **Underlying question still worth checking:** whether the Nour El Nil KV record actually has `name: "Nour El Nil"` set. If it doesn't, the symptom is patched but the data is still missing — fix in admin-property-editor. Quick check after deploy: DevTools → Network → load /pp-dashboard → find /api/property?slug=nour-el-nil → look at Response.data.name. |
| v75 | **Real partner identity via tb_pp_user_cache session storage — sidebar footer, dashboard welcome, pp-settings account card all now reflect the signed-in user (or blank when nobody is).** First letter rollover (v74z → v75) since the alphabet ran out. The underlying problem this fixes: Clerk only loads on `pp-login.html`; partner pages don't load Clerk after, so they had no access to the real user — leaving hardcoded "Mohamed Hassan / Property Manager" in the sidebar and other places where a real partner name should appear. **The chosen fix:** lightweight session-storage cache (key `tb_pp_user_cache`) written by `pp-login.html` when Clerk reports a signed-in user, read by partner pages. Mirrors the customer-side `tb_user_cache` pattern from v74w. Doesn't load Clerk on every partner page (kept lightweight; that's the next-up "real partner auth gate" task — see v74z notes), but does give us honest identity across the portal today. **Cache payload:** `{ id, name, email, avatarUrl, initials, signedInAt }`. **pp-login.html:** new `tbPpCacheUser()` helper at top of script section. Called on both redirect-to-dashboard paths — the already-signed-in branch (`if (window.Clerk.user)`) AND the fresh-sign-in branch (`addListener` callback). Always writes before `location.href` change. **assets/pp-sidebar.html:** hardcoded "MH / Mohamed Hassan / Property Manager" replaced with empty spans (`#pp-sb-avatar`, `#pp-sb-name`, `#pp-sb-role`). Wrapper `#pp-sb-footer-user` gets `opacity:0` + transition by default — invisible until populated. **assets/pp-sidebar-inject.js:** SIDEBAR_VERSION bumped to 2 (invalidates the v1 cache that has the old hardcoded markup). New `applyFooterUser()` function reads `tb_pp_user_cache` from sessionStorage; if present, populates the avatar (with initials fallback when no `avatarUrl`), name, and role (email shown as the secondary line), then sets `opacity:1` on the wrapper to fade in. If cache empty, footer stays invisible — matches Miguel's "leave it blank if no user signed in" instruction. **pp-dashboard.html:** `applyHeader()` now prefers partner first-name from `tb_pp_user_cache` over the property name. "Welcome back, {firstName}" when signed in; "Welcome back to {propertyName}" when no cache. Subtitle and topbar stay focused on the property. **pp-settings.html:** the v74x "sign out only" account card upgraded — when cache present, shows a full account row with avatar (real image if available, initials fallback), name (or email username if no name), email, and copy "Signed in. Manage account-level settings or sign out." When cache absent, falls back to the v74x signed-out state (sign-out button + helper note about managing identity via sign-in screen). Sign-out cleanup extended to also clear `tb_pp_sidebar_v2`. **Behavior contract:** | State | Sidebar | Dashboard | pp-settings | with cache → real user shown everywhere; without cache → all blank/property-name only; sign-out → cache cleared, redirect to pp-login. **Verify after deploy:** (1) sign out completely, navigate to /pp-dashboard — header reads "Welcome back to Nour El Nil"; sidebar footer is invisible; pp-settings shows the signed-out account state. (2) sign in via /pp-login — should redirect to dashboard, header now reads "Welcome back, {your-first-name}", sidebar footer fades in showing your avatar/name/email, pp-settings shows the full signed-in account row. (3) sign out via the pp-settings button — should redirect to pp-login and clear all the caches (verify in DevTools session storage panel). **Note on cache staleness:** if a user updates their Clerk profile in another tab, the cached version stays — refreshes only on next sign-in. Acceptable for v1; the future real-auth gate (next big partner-portal task) will replace cache reads with live Clerk reads. **What this does NOT fix:** anyone with the URL still bypasses sign-in entirely — there is no per-page auth gate on partner pages. That's the real security work, flagged earlier as the next architectural task. |
| v74z | **pp-dashboard.html rebuilt with real KV data.** Companion to v74y (pp-analytics rebuild) — same problem (worst no-fake-data violations in the portal), same fix (wire to real KV endpoints). Replaced: hardcoded "Welcome back, Mohamed"; four fake stat tiles (Enquiries this month 24 / Confirmed bookings 8 / Revenue via The Bearing $42,400 / Listing health 86%); three fake upcoming-booking rows (Sarah & James Alderton London UK Founding Member, Thomas Müller Munich, Patricia Chen San Francisco); two fake unread-messages snippets ("Could we arrange the private Edfu sunrise…", "Question about the dietary requirements…"); a fake listing-health checklist hardcoded to specific items at 86%. **What replaces it:** header reads "Welcome back to {property.name}" instead of a hardcoded name — partner identity still isn't wired (see v74x notes on Clerk gap), but using the property name from `/api/property?slug=PP_SLUG` is honest and useful. Four real KPI tiles: Enquiries this month (calendar-month boundary, with `↑/↓ N vs last month` delta showing real diff), Confirmed bookings (lifetime total of confirmed+completed — partners care about the booked total not a monthly cut, copy says "X total" not "↑Y vs last month"), Pipeline revenue (sum of `totalAmount` on confirmed bookings; shows "$0 — Activate Stripe to start collecting" when zero), Listing health (real % computed from property record field completeness, with color coding green ≥80 / amber ≥50 / red <50). Upcoming bookings table: real bookings with arrival in next 90 days, sorted ascending, max 5 rows, columns Guest/Dates/Cabin/Guests/Status, View buttons link to `pp-booking-detail?ref=X`, honest empty state when none. Recent messages card: real top-3 most-recent conversations by updatedAt with unread badge driven by `loopSummary.unreadPartner`, rows link to `pp-conversations?id=X`, honest empty state. Listing health card: full breakdown showing each of 9 essential property fields (name, tagline, location, starting price, editorial intro, hero photo, room types, tags, what's included) as ✓ or ○, with "Improve listing" button linking to pp-listing when items missing. Preview listing link in topbar: handles nour-el-nil → `/nour-el-nil` vs other slugs → `/property?slug=X`. **Implementation notes:** all date formatting done in browser (`toLocaleDateString`), all currency formatted via simple thresholds ($0/$X/$Xk/$X.XM), all XSS-safe via escapeHtml helper, all loads in parallel via Promise.all of 3 endpoints (`/api/property?slug=`, `/api/booking?slug=`, `/api/conversation?slug=`). **What this does NOT include:** real partner identity (still requires the partner-auth work flagged in v74x — meaningful next task). The "Welcome back to {property.name}" framing is the best we can do without it. **Verify after deploy:** open /pp-dashboard — header should say "Welcome back to Nour El Nil" (not Mohamed); KPI tiles should all show real zero-ish numbers with honest copy (since no real bookings yet); upcoming + messages cards should show honest empty states; listing health should show real percentage based on what's actually in the Nour El Nil KV record (probably ~90%+ since that record is well-populated). |
| v74y | **pp-analytics.html rebuilt with real data.** Previous state was the worst no-fake-data violation in the partner portal: hardcoded "1,847 page views (↑34%)", "Enquiry rate 3.2%", "Enquiry → booking 33% — Industry avg: 24%", "Avg review score 4.96 — based on 84 voyages", a 5-row Guest origin breakdown (UK 34%, US 28%, DACH 16%, France 10%, Other 12%), a 5-row Booking lead-time breakdown, and a 12-month revenue chart with "Total: $82,600". None of it was wired to anything. **What replaces it:** four KPI tiles driven by real KV data — Enquiries (count of `/api/conversation?slug=PP_SLUG`), Bookings (count of confirmed+completed from `/api/booking?slug=PP_SLUG`), Enquiry → booking conversion rate (suppressed below 3 enquiries since the ratio is noise), Pipeline revenue (sum of `totalAmount` on confirmed bookings only). A booking pipeline card showing real pending/confirmed/completed/cancelled split with values. A recent enquiries feed showing the most recent 5 conversations with guest name, message preview, and relative timestamp — each row links to `pp-conversations?id=X`. A booking lead-time histogram (5 buckets: 0–30 / 31–60 / 61–120 / 121–180 / 180+ days), but **hidden until ≥5 confirmed bookings exist** — below that, the distribution is noise. A "Coming as the platform grows" card honestly explaining what we can't show yet (page views — no web analytics wired; guest reviews — no review system; guest origin — we don't collect country data; industry benchmarks — too few properties). **Period filter wired:** topbar dropdown switches between Last 30 days / 90 days / 12 months / All time and all KPIs/charts re-render. **Honest empty states everywhere:** "No enquiries in this period yet", "No bookings yet. They'll appear here as enquiries turn into confirmed bookings", "Need at least 3 enquiries for a rate" (instead of misleading 0% or 100%). **Bug caught during build:** initially used `?propertySlug=` for the conversation endpoint — the actual param is `?slug=`. Fixed before packaging. Always worth checking real endpoint shape against assumed names (see pp-bookings, pp-conversations as references). **What this does NOT include:** real-data fix for the matching dummy stats on pp-dashboard ("Welcome back, Mohamed" hardcoded + four fake stat tiles 24/8/$42,400/86%). That's the next obvious cleanup — should be a small build given pp-analytics is now the template. **Verify after deploy:** open /pp-analytics — should see honest zero-state numbers across the board (since no real bookings yet exist) with appropriate empty-state messages, plus the "Coming as the platform grows" panel at the bottom. Switching the period dropdown should re-render the page. Lead-time card should NOT appear (we're below 5 confirmed bookings). |
| v74x | **pp-settings.html: hub redesign + honest account card.** Previous state: a "Coming soon" placeholder with two buttons (notification preferences → / dashboard ←). Audit revealed it wasn't actually broken — it was a deliberate empty state — but it also wasn't useful. The other three partner settings surfaces (pp-listing for property details, pp-notifications for email preferences, pp-team for team members) are all real, working pages. The right purpose for pp-settings is a hub that points to them, plus a sign-out control. **What's new:** page header ("Account & property / Settings"); three navigation tiles in a responsive auto-fit grid linking to pp-listing, pp-notifications, pp-team (each with a colored icon, title, description sentence, and a hover-animated arrow); a "Your account" form-section card with a Sign out button. **Honest about partner auth state:** during the build I discovered that partner pages don't actually load Clerk after pp-login. The "Welcome back, Mohamed" on pp-dashboard is hardcoded text; the sidebar footer "Mohamed Hassan / Property Manager" is hardcoded in `assets/pp-sidebar.html`. There's no real per-user session check on any pp-* page after login. I almost shipped a Clerk-driven account card showing "real" user name/email/avatar — would have shown placeholder text forever because Clerk never loads. Instead the account card is just a sign-out button with a small note explaining account identity is managed on the sign-in screen ("changing those today means signing out and back in"). **Sign-out behavior:** best-effort call to `window.Clerk.signOut()` if Clerk happens to be loaded (e.g. fresh from pp-login in the same tab); clears `tb_pp_sidebar_v1`, `tb_pp_user_cache`, `tb_pp_active_slug` from session/local storage; redirects to `/pp-login`. **What this does NOT do:** add real partner auth across the portal (separate task — sizeable, touches all 13 pp-* pages); add per-property settings beyond what already exists in listing/notifications/team; provide a way to change account email/password without going through pp-login. **Outstanding pp-settings gaps the hub does NOT solve, flagged for future builds:** (a) Real partner auth/identity wired into pp-* pages so we can show real user name/email + per-user preferences. (b) Stripe Connect payout setup card (deferred until Stripe activates). (c) Property-level "out of office" or extended closure dates (overlaps with pp-availability — needs design decision). (d) Multi-property switcher in the topbar (only relevant once a partner manages >1 property). **Verify after deploy:** sign in as partner, navigate to /pp-settings — should see three colored tiles (Property listing, Email notifications, Team members) with hover lift, then below them a "Your account" card with a Sign out button. Each tile click should navigate to its respective real page. Sign out should redirect to /pp-login and (verifying in DevTools) clear the session storage keys. |
| v74w | **Demo data cleanup across the site + Founding Member system: full infrastructure, customer-facing badges, founding-member.html live counter, admin UI for grant/revoke, and full removal of The Lens UI surfaces per Option C.** First half (demo cleanup): the 12 standalone demo property HTML files (al-moudira.html, amangiri.html, singita-grumeti.html, etc.) were already deleted in earlier builds, but their data references remained scattered. Full sweep: `KNOWN_PROPERTY_SLUGS` arrays slimmed from 13 to 1 (`['nour-el-nil']`) across 14 files; `CI_PROPERTIES` Envoy lookup dictionaries had 10 demo entries removed across 8 files; `vibe-search.html`'s PROPERTIES array stripped from 8 to 1; `search.html` lost 4 demo property cards (~23K chars); `nour-el-nil.html` lost the fake "Similar journeys" demo section (Aqua Nera/Gypsy/Chablé with "coming soon" toasts); `admin-property-editor.html` slug placeholder updated from `singita-grumeti` to `property-slug`. Second half (Founding Member): full state machine in `functions/api/envoy.js` (~400 lines: `foundingMemberReserve`, `foundingMemberVoidIfPendingForBooking`, `foundingMemberPromote`, `foundingMemberAdminGrant`, `foundingMemberAdminRevoke`, `foundingMemberRunPromotionScan`, plus counter helpers `fmLoadCounter`/`fmSaveCounter`/`fmAllocateNumber`). **Schema:** `member.foundingMember = { number, status: 'pending'|'awarded'|'voided', reservedAt, reservedForBookingRef, awardedAt, voidedAt, voidedReason }`. Counter at `__founding_member:state` with `nextNumber`, `reservedCount`, `awardedCount`, `voidedNumbers: [...]`, `cap`. **Number assignment:** smallest recycled number first, then `nextNumber++` — keeps the visible range compact when early bookings cancel. **Cap:** `FOUNDING_MEMBER_CAP = 1000` constant — soft-enforced (refuses new reservations at cap, no error thrown); bump constant to raise. **Hooks wired into ALL 4 deposit-paid paths** (rescue, sync-payment, webhook, $0-deposit-bypass for credits redemption) and ALL 3 cancellation paths (manual PATCH cancel, DELETE, stale-conversation batch). Promotion (`pending` → `awarded`) runs in a dedicated scan over `__members_index` in the hourly cron alongside the credits scan — independent of credits because Nour El Nil bookings are excluded from earning credits but DO count toward FM (a free trip is still a trip). Void on cancellation recycles the number AND attempts to re-reserve FM against another confirmed booking from the same member if one exists. **Once-and-done:** a member earns FM exactly once. Voided pending FMs from cancelled bookings get recycled; awarded FMs are never automation-revoked. **New endpoints:** `GET /api/founding-member/me?guestId=X` (guest read), `GET /api/founding-member/stats` (public — used by founding-member.html), `POST /api/founding-member/admin/grant` (admin-gated, requires reason), `POST /api/founding-member/admin/revoke` (admin-gated, requires reason), `POST /api/founding-member/admin/run-scan` (manual cron trigger). **Customer sidebar:** hardcoded `<span>✦ Founding Member #247</span>` replaced with `<span id="tb-fm-badge">Member</span>` across 7 customer pages (saved, preferences, conversations, bookings, lens, settings, my-account); `assets/account-sidebar.js` extended with a new `applyFmBadge()` (reads from sessionStorage `tb_fm_cache`) and `refreshFmStatus(userId)` (fetches `/api/founding-member/me`, caches, re-renders). Badge priority: awarded → `✦ Founding Member #N`; pending → `✦ Founding Member #N · pending`; no-FM fallback → `Member since YYYY` (year extracted from Clerk `user.createdAt`); final fallback → `Member`. **founding-member.html:** fake "247 / 753 remaining" counter replaced with live `id="fm-count"` and `id="fm-remaining"` spans driven by `/api/founding-member/stats`; progress bar wired to `id="fm-progress"`; breakdown subtext shows `X confirmed · Y pending trip completion`; intro paragraph rewritten to remove Lens/48-hour mentions; CTA paragraph updated to "Founding Member status is earned the moment you pay the deposit on your first Bearing booking — confirmed permanently once you complete your trip." **The Lens removal (Option C — Miguel's decision: hide UI surfaces, keep forward-looking marketing brand language elsewhere; NOT a Founding Member benefit):** customer sidebar Lens nav link removed from 6 customer pages; `collections.html` Founding Member promise updated (no more "Unlock The Lens forever") to focus on Reserve Credits path; `nour-el-nil.html` had three Lens surfaces removed — subnav `<a href="#sec-lens">The Lens</a>` item, the `dbc-lens-teaser` block in the book card (4 expert face circles), and the full ~250-line `<!-- ══ THE LENS ══ -->` section (expert selector pills + content panels + selectExpert script) AND the ~40-line `<!-- ══ LENS GATE OVERLAY ══ -->`; scroll-spy `secs` array updated to remove `'sec-lens'`. `property.html` had the `subnav-item id="sub-lens"` button, the entire `<!-- THE LENS -->` lens-section block, AND the LENS GATE OVERLAY removed; the dynamic Lens-rendering JS that populated those elements was stubbed (commented out — kept as reference; KV-stored lens data on property objects is untouched for future use). `search.html` lost the "The Lens available" filter pill from the features sidebar AND the "🔭 The Lens" card tag from the Nour El Nil card. `vibe-search.html` had its "Consulting The Lens on 214 properties…" loading message replaced with a neutral "Cross-referencing the curation library…". `lens.html` file deleted. `'lens.html'` filename references stripped from 14 files (these were in filename-lookup arrays used by routing logic). Marketing brand mentions of "The Lens" in `index.html`, `the-bearing.html`, `about.html`, `the-envoy.html`, `collections.html` left intact per Option C — they're forward-looking copy, not functional promises. **Founding Member benefit list** rewritten on founding-member.html: removed The Lens benefit entirely; softened "48-hour early access" to "First access to future perks (coming soon)"; kept Reserve Credits ($4,000 → free Nour El Nil cabin) and permanent badge/number. **Admin UI:** `admin-guests.html` has a new "Founding #" column between Tier and Location showing `✦ #N` (gold, weight 600) for awarded members, `#N pending` (muted) for pending, `—` otherwise; colspan updated to 10 across all error states. `admin-guest-detail.html` got a new "Founding Member status" card after Profile, with a live status display (shows full state: number, status, awarded date, granted-by actor, reserved booking ref) and Grant / Revoke buttons that hit the admin endpoints — both require a typed reason via prompt, capture the Clerk admin's email as actor, disable appropriately by state (grant disabled when awarded, revoke disabled when not enrolled), call `load()` to refresh after success, show toast confirmation. **Specifically for Miguel/mcancino@gmail.com:** the "#243" / "#247" you were seeing was hardcoded in 7 sidebar files — NOT in any database. After this deploys, you'll see "Member since 2026" (or whatever year Clerk has for your account) until you grant yourself FM #1 via admin-guest-detail. No database record yet has `foundingMember` set. **No retroactive grants for pre-v74w bookings**, same policy as Reserve Credits. **What v74w does NOT include:** retroactive FM grants for historical bookings; email notification when FM status awarded (worth adding later); cap-extension UI (still requires editing `FOUNDING_MEMBER_CAP` constant in worker); building real Lens content (deferred); the 48-hour early-offer-access functionality (no offer broadcast system exists yet — the copy is now appropriately forward-looking). **Verify after deploy:** (1) sign in as yourself — sidebar should now read "Member since 2026" (not "#247"); (2) open admin-guests — there's a new "Founding #" column showing `—` for everyone since no FM records exist yet; (3) click your own member row → admin-guest-detail → Founding Member status card → click "Grant Founding Member", enter a reason like "founder grant", you should see ✦ #1 awarded; (4) refresh any customer page — sidebar should now show "✦ Founding Member #1"; (5) navigate to founding-member.html (signed out or in) — counter should show "1 of 1,000" with the breakdown "1 confirmed · 0 pending trip completion"; (6) navigate to /lens — should 404 (the file is gone); (7) check nour-el-nil.html — the subnav should NOT have "The Lens" between Gallery and Cabins; (8) search.html should not have the "🔭 The Lens" tag or "The Lens available" filter pill. End-to-end test of FM-via-deposit-paid requires a real Stripe-confirmed booking, which is still stubbed — the hooks will fire correctly once Stripe goes live. |
| v74v | **Reserve Credits system — full implementation.** Substantial single build adding the loyalty mechanic Miguel specced: every Bearing booking earns 10% pending credits when deposit is paid; pending promotes to earned when full balance paid AND checkout+7days passed; earned credits redeemable in $4,000 blocks against Nour El Nil bookings only; Nour El Nil itself doesn't earn (it's the redemption property); credits expire 24mo after last earned event; cancellations void any credits earned from that booking and reverse any redemption applied to it. **Worker (`functions/api/envoy.js`):** ~340-line credits module with idempotent ledger model (state machine pending→earned→used, voided/expired as terminal states; balance always computed from ledger never stored separately); helpers `creditsAccrueOnDeposit`/`creditsPromotePending`/`creditsVoidOnCancellation`/`creditsRedeem`/`creditsReverseRedemption`/`creditsExpireStale`/`creditsRunPromotionScan`; member resolution via `booking.guestId` (added v74v) with email-lookup fallback through `__members_index`; hooked into all three deposit-paid handlers (rescue path, customer sync-payment, Stripe webhook), all three cancellation paths (manual PATCH, DELETE, stale-cron batch), and `/api/checkout/create-intent` redemption path; new endpoints `GET /api/credits/me?guestId=X` (guest read), `GET /api/credits/admin` (list view) + `?memberId=X` (drill-down), `POST /api/credits/admin/adjust` (manual ledger entry with required reason), `POST /api/credits/admin/redeem` (manual offline-redemption marking), `POST /api/credits/admin/run-promotion` (manual scan trigger); cron wired — `scheduled` handler now runs `creditsRunPromotionScan` hourly alongside `runStaleConvReminders` (idempotent so hourly is fine); $0-deposit Stripe-bypass path — when credits cover full booking (rare: Nour El Nil offer ≤$4K) the worker confirms booking directly without creating a Stripe PaymentIntent, returns `skipStripe: true`. **Customer UI:** my-account.html Reserve Credits stat tile and progress card both wired to live data via `/api/credits/me`, removed fake "$420 / $3,500" hardcoded values, added pending-display ("+ $X pending — earned officially after each trip completes"), goal updated to $4,000; conversations.html offer-acceptance modal now checks if offer is Nour El Nil AND guest has ≥$4K earned, renders "Apply $4,000 Reserve Credits" checkbox with live price preview (new deposit / new total); `assets/stripe-inline.js` `payDeposit()` extended to accept `applyCredits` and `applyCreditsMemberId` config — forwarded to `/api/checkout/create-intent` — and handles `skipStripe:true` response by showing success state and calling onSuccess; nour-el-nil.html got an exclusion notice near book button ("Reserve Credits: bookings on Nour El Nil don't earn credits, but you can redeem $4,000 in earned credits for a free standard cabin or discount on premium"); nour-el-nil.html booking POST now includes `guestId` from `window.Clerk.user.id`. **Admin UI:** new `admin-credits.html` (~480 lines, follows admin shell pattern): summary tiles (members with credits / total pending / total earned / ready-to-redeem at $4K+); members table sortable by earned descending; click any member to open drill-down modal with full ledger, balances, expandable manual-adjust form (amount + status pending/earned + required reason, signed by Clerk admin email), expandable manual-redemption form for offline use; "Run promotion scan" button in topbar triggers the cron manually; new sidebar entry "Reserve Credits" added to `/assets/admin-sidebar.html` between Payments and Analytics, with SIDEBAR_VERSION bumped from 3→4 to invalidate cached partials. **$3,500 → $4,000 sweep:** 9 replacements across founding-member.html, about.html, index.html, the-bearing.html, my-account.html (search.html's per-property "$420 Reserve Credits" hover stats on individual property cards were intentionally NOT touched — those are forward-looking 10% estimates on demo data and live alongside abandoned scaffolding properties; flag for cleanup in a separate sweep). **Key design decisions locked (full discussion in chat):** Stripe webhook is canonical deposit-paid trigger but `sync-payment` + rescue path also fire — all idempotent on `(bookingRef + 'deposit_paid')`; redemption marks the ledger only at payment-success, not at intent-creation (no burn on abandoned checkouts); expiry clock resets only on `earned` entries not `pending` (anti-gaming); admin-adjust endpoint exists for future Founding Member welcome-grant operation (intentionally no auto-backfill in v74v — historical bookings get $0 by default, retroactive grants done explicitly via admin-credits manual adjust). **What v74v does NOT include:** retroactive crediting of pre-v74v bookings (use admin-credits → adjust if you want to grant founding-member starter credits); email notifications when credits earned/expiring (worth adding later); partial redemption below $4,000 (your spec was all-or-nothing); credit transfers between accounts. **Verify after deploy:** (1) my-account Reserve Credits card shows $0 earned + 0% progress for any signed-in user who has no booking history (no more fake $420); (2) goal everywhere reads $4,000; (3) Nour El Nil property page shows the exclusion notice; (4) admin sidebar has Reserve Credits link → admin-credits page loads and shows empty state ("No members have earned credits yet") since this is fresh; (5) manual scan button works without erroring. End-to-end test requires a real Stripe-confirmed booking, which Miguel hasn't set up yet (Stripe still stubbed) — the worker hooks will fire correctly the moment Stripe goes live. |
| v74u | **Partner navigation flash: third attempt, grounded in concrete code differences.** Miguel instructed me to compare admin/customer to partner line-by-line and find the actual structural difference. Done. **The single concrete structural difference between portals:** admin pages have THREE render-blocking `<script>` tags in `<head>` (Clerk SDK + admin-gate.js + admin-fetch.js + admin-sidebar-user.js). Customer pages have TWO inline render-blocking `<script>` tags in `<head>` (the 8-second auth failsafe and the cached-user pre-paint populator I touched in v74r). **Partner pages had ZERO `<script>` tags in `<head>`.** All other structural attributes I compared are identical or close: `<aside class="sidebar">` with the same position:fixed CSS, identical sidebar markup (after v74s), same `portal-chrome.css` link, same body structure (`<div class="app">` wrapping sidebar + main-area), same clean URLs in nav. **The hypothesis:** when a navigation begins, the browser needs to know whether the destination page opts into view transitions. The opt-in is registered when `portal-chrome.css` is parsed (it contains `@view-transition { navigation: auto }`). On admin/customer pages, the head scripts force a parse-pause that gives `portal-chrome.css` time to fully load and register the opt-in before navigation can complete. On partner pages, with zero head scripts, there's nothing forcing that pause — the navigation can race ahead and complete before the opt-in is registered, causing the browser to skip the view transition and produce a hard cut/flash. **Fix:** added one render-blocking inline `<script>` to the `<head>` of all 13 partner pages, just before `</head>`. The script does two useful things: (1) forces the parse-pause that's the actual hypothesized fix, and (2) pre-warms the sidebar mount by reading `sessionStorage['tb_pp_sidebar_v1']` and registering a MutationObserver that injects the cached sidebar markup the INSTANT the empty mount element appears in the DOM (slightly earlier than the body-level inject script). The script is idempotent and degrades safely if the cache is empty (the body-level inject script then takes the normal path). **Important: this is grounded in evidence, not confirmed.** The actual cause-and-effect chain — does a render-blocking head script reliably resolve the @view-transition opt-in race? — is something I can't verify without testing in a real browser. The structural fact (head scripts on admin/customer, none on partner) is real and reproducible. The causal claim is plausible but unverified. If v74u doesn't fix it, that rules out this hypothesis decisively and the next step is real diagnostic data from Miguel's browser (DevTools Network panel showing 200 vs 3xx on sidebar clicks; DevTools Performance recording showing where paint happens vs CSS load; whether the flash is white/dark/content-slide). **What I am NOT shipping:** another speculative theory dressed up as a fix. This build documents the exact reasoning, the unverified causal step, and the diagnostic data needed if it doesn't work — so the next iteration starts from ground truth instead of more theory. **Verify after deploy:** (1) navigate between partner pages — does the flash stop? If yes, ship it. If no, proceed to (2). (2) open DevTools → Network panel, click a sidebar link, look at the FIRST response: is it 200 OK or 3xx redirect? (3) DevTools → Application → Session Storage: is `tb_pp_sidebar_v1` present after a couple of navigations? (4) does the flash look white, dark, or like content sliding in? Even ONE of those data points kills 90% of the remaining theory space. |
| v74t | **Partner portal: duplicate heading blocks removed from 10 pages. Partner-flash investigation paused pending diagnostic data.** Miguel reported that nearly every partner page (except conversations, which was fixed in v74o) had a duplicate type-heading stack — e.g. on pp-experiences: "Private Access experiences / Exclusive experiences only The Bearing guests can book." appeared in the topbar AND in a kicker block right below it. Same pattern as the conversations page that v74o fixed: every partner page has both a topbar (`.topbar-title` + `.topbar-sub`) AND a `.page-header` kicker block (`.page-eyebrow` + `.page-title` + `.page-subtitle`) directly underneath, and on most pages the kicker title duplicates the topbar title. **Audit results across 11 main partner pages + pp-booking-detail:** 10 pages had pure duplication (photos, rooms, experiences, bookings, availability, analytics, team, notifications, settings, booking-detail). 2 pages (pp-dashboard, pp-listing) had a kicker with DISTINCT content from the topbar (dashboard has "Welcome back, Mohamed" as the kicker title vs "Dashboard" in topbar; listing has "Edit your listing" vs "Edit listing"). Those 2 were left alone. **pp-notifications had the kicker block TWICE** (lines 184-188 and 189-193) — both were removed, the more informative subtitle ("Choose which emails you receive about this property...") promoted to the topbar-sub. **Approach:** for each of the 10 duplicate pages, the kicker subtitle (which tended to be more detailed than the topbar-sub it duplicated) was folded into the topbar-sub, then the `.page-header` kicker block was deleted entirely. Net: one header per page, the more informative subtitle preserved, no duplication. Pages structurally now match other partner pages and pp-conversations.html. **Partner navigation-flash issue STILL OPEN — needs diagnostic data.** Miguel reports v74s did not fix the flash. v74r's customer-side fix landed cleanly (confirmed working) but v74r's partner-side investigation was inconclusive, and v74s's "byte-identical sidebar via injection" theory also did not solve it. Two failed theories means it's time to stop guessing and get evidence. **What I need from Miguel to fix this properly:** (1) Which specific page-to-page click flashes? (e.g. dashboard → bookings, photos → rooms, rooms → settings) — is it ALL navigations or just some? (2) Visually, what does the flash look like? White frame between pages? Dark frame? Content sliding in from the side? Content fading in? Layout shift / jump? (3) Browser + version (Chrome 137, Safari 18.3, etc.) and OS? (4) DevTools → Network panel: when you click a sidebar link, is the new page returned 200 directly, or is there a 301/302 redirect first? (5) DevTools → Application → Session Storage: does `tb_pp_sidebar_v1` exist with a JSON value after navigating around? (6) Does the flash happen on EVERY click, or only the first few? (7) Does hard-reloading (Cmd+Shift+R) and then clicking change anything? Without this, any further attempt will be another speculative fix. **Verify after deploy:** open any of the 10 partner pages (e.g. pp-experiences) — you should see a single header row at the top (topbar) with "Private Access experiences" as the title and the more detailed "Add exclusive experiences — these are a key differentiator on The Bearing." as the subtitle. No second stack of heading text underneath. pp-dashboard and pp-listing intentionally still have their second header block (welcome message / edit instructions) because those aren't duplicates. |
| v74s | **Partner portal: cached sidebar partial (admin pattern adopted) to fix navigation flash.** Follow-up to v74r. v74r fixed the customer-portal flash but couldn't identify the cause of the partner flash from static evidence — the obvious suspects (opacity:0 rules, visibility-hidden gating, font load differences, sidebar position) were all clean on partner. The likely remaining cause was **byte-different sidebar markup across the 12 partner pages**, even after normalizing the `active` class and stray `id` attrs. View transitions match elements by `view-transition-name`, but the snapshots they capture are pixel-level, and small markup differences can produce subtle morph artifacts the user reads as a "flash". Admin solved this in v72n by extracting the sidebar to a static partial (`assets/admin-sidebar.html`) and injecting it on every page from a sessionStorage cache via `admin-sidebar-inject.js` — every admin page has byte-identical sidebar HTML, view-transition snapshots match perfectly, no flash. **This build adopts the same pattern for partner.** New files: `assets/pp-sidebar.html` (canonical partial, extracted from pp-dashboard's inline markup with `active` class stripped and `data-page` attrs added to each `.sb-item`) and `assets/pp-sidebar-inject.js` (mirrors admin-sidebar-inject.js — fetches once per session, caches in `sessionStorage['tb_pp_sidebar_v1']`, injects synchronously on cache hits). All 13 partner pages (the 12 main pages + pp-booking-detail) had their inline `<aside class="sidebar">…</aside>` replaced with `<aside id="pp-sidebar-mount" class="sidebar"></aside>` + `<script src="/assets/pp-sidebar-inject.js"></script>`. Each `<body>` got a `data-page="pp-XYZ"` attribute so the injector can mark the right link active. The `?as=X` partner-impersonation rewrite that previously lived in `pp-sidebar-badges.js` was duplicated into the inject script so it fires the moment the sidebar appears (badges.js's rewrite is idempotent so the duplication is harmless). **Cache lifecycle:** first partner page load this session → fetch + cache the partial; every subsequent partner navigation → inject synchronously from cache before paint. The `.sidebar` CSS rule already sets `background:var(--sidebar-bg)` (dark) on the empty mount, so even during the first-load fetch there's a dark column rather than a white flash. **Page weight:** removed ~4.9KB of inline sidebar HTML from each of 13 pp pages; net saving on cached navigations is ~64KB across the partner portal. **Verify after deploy:** sign in to partner portal, navigate dashboard → listing → photos → rooms → bookings → conversations → availability → analytics → team → notifications → settings. The sidebar should stay perfectly still across all clicks (already the case from view transitions; should now be even cleaner since the bytes match exactly). The dark sidebar background should never disappear between navigations. The current page should always have its sidebar link marked active. The "Nour El Nil" context block at the top of the sidebar should be present on every page (currently hardcoded in the partial — when a second real partner property is onboarded, the partial will need to become JS-populated or per-property like admin does for its property index). **Known limitations:** (a) On the very first partner page load of a session (before the cache is populated), the inject script does a network fetch of the partial. During that ~50-200ms, the sidebar mount is empty (but dark, so no white flash). Acceptable trade-off — this only happens once per session. (b) `pp-sidebar-badges.js` still queries `.sb-item` on DOMContentLoaded; on first session load, DOMContentLoaded may fire before the fetch completes, so the first badge query silently no-ops. The 4-second polling catches it within a few seconds. Inject also calls `window.refreshPpBadges()` after injection but that's a no-op if badges.js hasn't run yet on the first load. (c) `pp-login.html` was intentionally left untouched — the login page shouldn't share a cached sidebar with the rest of the portal (different auth state). **Going forward:** when changing the partner sidebar (adding/removing nav items, changing the context block), edit `assets/pp-sidebar.html` AND bump `SIDEBAR_VERSION` in `pp-sidebar-inject.js` so existing browsers fetch the new copy. |
| v74r | **Customer-portal navigation flash — fixed.** Investigation requested by Miguel: "menus for the customer and partner portals are reloading/flashing upon menu clicks (except conversations) when they were previously working." Phase 1 of a two-phase investigation; this build ships the customer fix. **How admin avoids flash (the working reference):** Two layers. (a) Cross-document view transitions in `assets/portal-chrome.css` — `@view-transition { navigation: auto }` + `.sidebar { view-transition-name: portal-sidebar }` make the browser cross-fade between pages instead of hard-cutting, and treat the sidebar as the same element across pages so it stays still while content fades. (b) Sidebar markup is byte-identical across all admin pages because it's an empty `<aside id="admin-sidebar-mount" class="sidebar"></aside>` populated by `assets/admin-sidebar-inject.js` from a sessionStorage cache. View-transition snapshots of admin sidebars match perfectly → zero perceived movement. **What customer pages do differently:** Six customer pages (`my-account`, `bookings`, `lens`, `saved`, `preferences`, `settings`) have a static CSS rule `.account-wrap { opacity: 0; transition: opacity .15s ease-in }` plus an `<div class="account-wrap">` element wrapping the main content. A `<script>` in the page sets that to opacity:1 once Clerk auth confirms. `conversations.html` does NOT have this rule, which is exactly why it's the only customer page that doesn't flash on navigation. The opacity-fade was originally there (v73g) to prevent a "flash of account content during sign-out → sign-in transition" but on every NORMAL signed-in navigation it produces a visible 150ms fade-in over the top of the view-transition. **Fix:** the inline pre-paint script (already present on each of the 6 pages, used to populate the sidebar profile from cache) was extended to also inject `.account-wrap{opacity:1!important;transition:none!important;}` whenever the session cache exists. With `!important`, this overrides the static `.account-wrap{opacity:0}` rule before the browser ever paints. Signed-in users now get instant full-opacity content on every navigation — no fade. First-visit users (or those who cleared session) still hit the original opacity:0 → opacity:1 fade-in protection, since the override only fires when cache is present. Six lines changed across 6 files (one `s.textContent=...` line per page). Existing fade-in JS (at line ~400 of each file) and its 8s failsafe remain in place for the no-cache case. **Partner side — needs user verification before action.** Audited all 12 partner pages: NONE have an analogous `.account-wrap{opacity:0}` rule, no `visibility:hidden`, no post-load opacity flips. Sidebar markup is identical across pages once you normalize the `active` class and a couple of stray `id` attrs on `sb-context-*` elements (which view-transition mechanics don't care about). Font loading is identical across all 12 pp pages (already unified in v74n). Body styles, sidebar CSS, sidebar widths — all identical. View transitions infrastructure (portal-chrome.css) is correctly linked. Within the static evidence I cannot identify a flash cause for partner pages. Possible explanations: (a) Miguel may be perceiving the standard 140ms view-transition cross-fade as a "flash" on partner pages, in which case the fix would be tuning that duration in portal-chrome.css. (b) Browser cache state from a prior build may be making partner navigations re-fetch some resource. (c) Something happens in DevTools or a specific browser that doesn't show in static inspection. **Action:** Miguel, after deploying v74r and hard-refreshing, please confirm (1) customer pages no longer flash on nav clicks (this is the part I'm confident about), and (2) whether partner pages still flash. If yes on (2), let me know which specific pp pages and what the flash looks like (white frame? gray frame? content slide?) so I can target it correctly. **Verify after deploy:** sign in as a customer, navigate dashboard → bookings → preferences → settings → bookings. There should be no visible content fade-in on any of those navigations. The sidebar should stay perfectly still (already the case from view transitions). Open DevTools → Application → Session Storage → check that `tb_user_cache` has a JSON value. If it does, the override fires; if it's empty (e.g. after sign-out), the fade-in protection still works. **Known limitations:** the fix only applies to signed-in users with a populated session cache. The very first navigation of a session (before Clerk loads and writes cache) will still show the original opacity:0 → 1 fade. This is intentional — flash protection during initial auth is worth the one-time fade. |
| v74q | **Email-notify icon: bell → envelope.** Follow-up to v74p. The bell icon I chose for the per-conv email-notifications toggle was ambiguous — a bell more commonly reads as "mute web/in-app notifications" rather than "control email delivery." Swapped for an envelope icon (closed envelope when emails are on; envelope with diagonal slash when muted). Universal email metaphor, identical 16×16 SVG footprint, same 32×32 button. Applied across all three conversation surfaces (admin, partner, customer) for consistency. Tooltip ("Email notifications on for this conversation — click to mute" / "muted — click to unmute") was already correct and is unchanged. Variable names renamed `bellOn`/`bellOff` → `envelopeOn`/`envelopeOff` for clarity. **Verify after deploy:** on any chat surface, the small square button in the conversation header now shows an envelope icon (not a bell). Click it → slash appears across the envelope, indicating muted. Click again → slash disappears. Tooltip on hover still says "Email notifications…" so the on-screen affordance + tooltip + label all align. **Known limitations:** the envelope-with-slash for "off" is the standard email-muted convention, but a user with poor color vision will rely entirely on the slash to read state since the on/off colors are close. The diagonal slash is bold (2px stroke) and corner-to-corner so it's unambiguous even at small size. |
| v74p | **Chat header polish + scroll-control overlap fix across all conversation surfaces.** Three fixes. **(1) Email-notifications toggle shrunk from verbose pill to icon-only bell button.** The previous pill ("Email notifications [Off | slider | On]") took ~200px of header real estate for a single boolean per-conversation preference — too prominent for what it does, and a major contributor to the header crowding the customer reported. Replaced with a 32×32 square icon button: bell SVG when on, bell-with-slash SVG when muted; tooltip describes both the current state and the click action. Same compact footprint as the gear button next to it on the partner side. Applied identically to admin, partner, AND customer conversations — the pattern was the same on all three, would have been a visible inconsistency to fix only two. **(2) "Sun" icon on partner-portal conversations is now an actual gear.** The button on the partner conversation header that opens the per-event notification preferences popover used an SVG that LOOKED like a sun — a center circle with eight cardinal/diagonal rays. It was labeled "gear" in code comments but rendered as a sun, which read as a theme/light-mode toggle rather than a settings control. Swapped the SVG for an actual cog/gear shape (standard settings icon) so it visually matches its purpose. **(3) New-messages pill + jump-to-bottom button no longer overlap the composer.** From v74m: ConvScroll positioned the controls at `bottom:96px` inside a positioned-ancestor walk-up, which usually resolved to the whole chat column (containing both messages and composer). `bottom:96px` was a guess at "above the composer height," but with varying composer heights and different page layouts across the three portals, the controls drifted over the reply box. Root cause: the controls should sit relative to the MESSAGES area, not the whole column. **Fix:** ConvScroll.install now wraps the messages-wrap in a non-scrolling `position:relative` stage div at install time. The messages-wrap goes inside the stage; the pill + button get `bottom:16px` relative to the stage. The composer is a sibling of the stage, never overlapped, regardless of how tall it grows. Reparenting is idempotent — repeated installs on the same wrap reuse the existing stage. Install now runs BEFORE pin-to-bottom in the finalize helpers, so the reparenting doesn't reset scrollTop. **Verify after deploy:** (1) open any conversation on admin/partner/customer — the email-notify control is a small square button with a bell icon (no more verbose pill); hover the icon, tooltip shows current state and action; click it, icon toggles between bell and bell-with-slash; (2) on the partner side, the icon to the right of the bell is a proper gear shape (not a sun); click it, the per-event preferences popover opens as before; (3) scroll up inside any conversation past the composer — the persistent jump-to-bottom button sits at the bottom-right of the MESSAGES area, NOT overlapping the composer/reply box; have someone send a new message while scrolled up — the "↓ N new messages" pill appears in the same spot, also not overlapping the composer. **Known limitations / open items:** (a) the stage-wrapping changes a small layout detail: the messages-wrap is no longer a direct child of the thread/column, but a grandchild via the stage. CSS that targeted `.thread > .ch-msgs` (with `>` direct-child) would break — but none in the codebase use that selector pattern, all use bare class selectors. (b) The stage is left in the DOM when ConvScroll.destroy is called — only the pill/button are removed. This is intentional (next install reuses the stage), but leaves a `<div class="conv-scroll-stage">` orphan element if a page calls destroy without a follow-up install. No visible effect; flagging for completeness. (c) The bell icon's "off" state uses a lighter gray color (#94a3b8 on admin/partner, #9a8e80 on customer) so it doesn't compete with the bell-on state visually. If you want muted notifications to be MORE prominent (so partners notice they've silenced a thread), happy to invert that. **New rule going forward:** apply command will always be at the very bottom of the response, directly above the download. |
| v74o | **Partner portal: privacy fix (guest emails never exposed) + duplicate page header removed + thread header crowding fixed.** Two issues, one of which is a meaningful privacy/business policy violation. **(1) Guest emails exposed to partners — CRITICAL.** The whole reason The Bearing sits in the middle of guest↔property communication is so the partner can't reach the guest outside the platform. The partner portal was leaking the guest's email address in four user-visible places: (a) conversation thread header subline ("`mcancino@gmail.com · Luxury Room · Online`"); (b) right-side guest context panel ("`Miguel Cancino` / `mcancino@gmail.com`"); (c) booking-detail modal under guest name; (d) bookings table guest cell as a secondary line under the name. All four removed. Also patched the more-subtle leaks: (e) initials-calculation for avatars used `guestName || guestEmail || '?'` as a fallback chain — meaning a guest with no name on record would have their email's first 2 letters appear in the avatar circle; changed to `guestName || '?'` everywhere; (f) display-name fallback `guestName || guestEmail` changed to `guestName || 'Guest'` (or `'guest'` lowercase where the context calls for it, e.g. inside a sentence); (g) bookings-table search haystack included `b.email` — removed (partners can't see emails, no point letting them search by typing one and identifying rows); (h) orphan `.context-email` CSS rule deleted. Net: there is no path through the partner UI that surfaces the guest's email — not display, not hover, not search, not derived initials. Admin portal is unaffected and still sees emails (founder runs the platform). Customer is unaffected and still sees their own. **(2) Duplicate page header on pp-conversations.** The page had a topbar with title "Conversations" + sub "3 unread threads" (hard-coded false data), AND directly below it a second header block with kicker "GUEST COMMUNICATIONS" + title "Conversations". Two header blocks stacked, with the topbar's subtitle showing a number that wasn't actually computed. Removed the lower block entirely and replaced the topbar's hard-coded sub with the more useful "Guest communications" descriptor — single header now, matches the structural pattern of other partner pages (each has one topbar with title + contextual sub). **(3) Conversation thread header crowding.** With the email gone from the subline, the title block no longer wraps to extra lines and stops overlapping the "Email notifications" toggle. Also added `white-space:nowrap; text-overflow:ellipsis` to `.thread-title` so a very long guest name stays on one line (with ellipsis) rather than wrapping into a multi-line title that crowds the actions area. **Verify after deploy:** (1) open pp-conversations as a partner — only ONE header block visible at the top ("Conversations / Guest communications"), no "3 unread threads" anywhere; (2) open any conversation thread — the header shows guest name + cabin only, NO email visible; (3) check the right-side context panel — guest name, presence, member-since, location, enquiry details, but no email; (4) open pp-bookings — the table guest cell shows just the name, no email subline; click any booking to open the detail modal — guest block shows name only, no email; (5) bookings search — typing a guest's email returns no matches (the search field no longer indexes email); typing their name still works. **Admin/customer side untouched** — admin still sees full guest details including email; customer still sees their own email. **Process note:** worth treating "guest data exposure on the partner side" as a recurring audit category. The leaks here came from copy-paste of the admin-page rendering patterns into partner pages without thinking through the trust boundary. Future partner-side features should default to "render minimum identifying info" and explicitly justify anything more. |
| v74n | **Admin sidebar header font shifting across pages — fixed.** When navigating between admin pages (dashboard, properties, conversations, bookings, etc.), the sidebar header text ("The Bearing", "Admin Portal", "The Bearing Team", "Internal · Full access") visually shifted size/width/weight. **Root cause:** the sidebar partial is cached and shared (so HTML is identical), and every admin page redeclares the same CSS for those classes (verified — identical across all 10 admin pages). But the Google Fonts `<link>` differed per page. Most admin pages loaded `Fraunces + DM Sans + JetBrains Mono`. Three pages — `admin-conversations.html`, `admin-bookings.html`, `admin-vectorize.html` — loaded `Instrument Serif + Geist` instead. The sidebar CSS uses `font-family:'Fraunces'` for the brand name, so on those three pages Fraunces wasn't loaded and the browser fell back to the generic `serif` (a Times-ish typeface) — completely different glyph widths. Same problem for the body text: `body{font-family:'DM Sans',...}` was declared on every page, but on those three the page didn't load DM Sans either, so all body text on those pages fell back to `system-ui`. The result: every navigation between a "Fraunces+DM Sans" page and an "Instrument Serif+Geist" page produced a visible font swap in the sidebar header (and elsewhere). `admin-property-editor.html` was a partial outlier — loaded Fraunces + DM Sans + Instrument Serif. **Fix:** unified the Google Fonts `<link>` across all 12 admin HTML files to load one combined URL with all five families that the admin portal collectively uses: Fraunces, DM Sans, Geist, Instrument Serif, JetBrains Mono. Net: each admin page makes one (now identical) fonts.googleapis.com request the first time it's visited in a session; subsequent admin navigations hit the browser cache. All sidebar headers, page titles, and body text render in their declared fonts on every page. **Verify after deploy:** navigate between admin-dashboard → admin-conversations → admin-bookings → admin-vectorize → admin-properties — the sidebar header should look pixel-identical at each stop (no shift in width or weight of "The Bearing", "Admin Portal", "The Bearing Team", or the subtitle). Also: body text on admin-conversations / admin-bookings / admin-vectorize should now render in DM Sans (consistent with the other admin pages) rather than the OS default sans. **Process note:** the v72n cached-sidebar refactor solved DOM-level consistency by extracting the markup to a shared partial. But it didn't reach into per-page font-loading, which is where the visible inconsistency still lived. Worth treating "per-page font imports" as part of the same shared-resource discipline as the sidebar partial itself — long-term, this should live on a single CSS file loaded by every admin page, alongside `portal-chrome.css`. **Known limitations / open items:** (a) Every admin page now loads ~5KB more of font CSS (one extra family at a few weights). Negligible. (b) The font-display: swap behaviour still applies — on initial page load, the user sees the fallback briefly before the real font swaps in. That's a one-time event per session, and it now happens identically on every admin page (which is the point — consistency, not elimination). To eliminate even that initial swap, would need `font-display: optional` or preload, which is a separate polish task. (c) Body content fonts on admin-conversations / bookings / vectorize were previously falling back to system-ui (the Geist family was declared on body but body says DM Sans; neither resolved correctly because of which fonts were loaded). Body text on those pages will now visibly change to DM Sans — a one-time visual shift for users who had been seeing the system font there; this is a correction, not a regression. |
| v74m | **Chat UX polish across all 3 surfaces: new-messages pill, jump-to-bottom button, and unread-messages divider.** Builds on v74l's scroll-respect fix. The 3 features work together so anyone returning to a conversation lands in the right place AND knows when there's new content arriving they haven't seen. **(1) "↓ N new messages" pill.** Appears in the bottom-right of the messages pane when (a) user is scrolled up beyond the threshold AND (b) new messages from someone other than the viewer arrive via the 5-second poll. Click → smooth-scrolls to bottom and dismisses. Auto-dismisses when user scrolls back within 80px of bottom on their own. Terracotta gradient, matches brand. **(2) Persistent jump-to-bottom button.** Subtle white circle with down arrow, same position. Appears whenever the user is more than 200px from the bottom, regardless of whether new messages have arrived. Click → smooth-scrolls to bottom. Hides when within 200px. Pill takes priority when both would show. **(3) Unread-messages divider.** When a user opens a conversation that has unread messages, a horizontal "New messages" pill divider is inserted in the message list BEFORE the first unread message, and that divider is scrolled into view instead of the bottom. Lets the user pick up exactly where they left off in a busy thread. Divider only renders on the initial open; refresh polls don't re-insert it. **Architecture:** the pill + button live in `assets/conv-scroll.js` as a new `ConvScroll.install(wrap, opts)` API that returns `{ showNewMessagesPill(count), hideNewMessagesPill(), destroy() }`. Single source of truth — all 3 conversation pages install once on `openConv`, destroy on next open. Scroll-position tracking + show/hide logic + smooth-scroll all handled inside the helper. Each page (customer `conversations.html`, admin `admin-conversations.html`, partner `pp-conversations.html`) gets: (a) `*ScrollControls` + `*UnreadAtOpen` state vars; (b) `openConv` captures unread count before the mark-read PATCH wipes it, destroys any previous controls; (c) `renderMessagesInto` inserts unread divider at index `(messages.length - unreadAtOpen)`; (d) new `*FinalizeOpenScroll(wrap)` helper called at the end of `renderThread` for both archived and normal branches — scrolls divider into view if present, otherwise pins to bottom, then installs ConvScroll, then resets `*UnreadAtOpen` to 0; (e) `refreshMessages` calls `*ScrollControls.showNewMessagesPill(toAnimate)` when new-from-other arrives. CSS for the divider matches each page's palette: cream/terra on the customer side, the accent terracotta on admin and partner. **Title/favicon alerts already exist** — `ConvPresence.setUnreadInTitle()` and `drawFaviconDot()` from `assets/conv-presence.js` are already wired across all 3 surfaces, so cross-tab unread notifications (`(3) New messages!` blinking in tab title + green dot on favicon) are already in place; this build didn't need to touch that. **Verify after deploy:** (1) on any chat surface, scroll up to read older messages, have another party send a message — a terracotta "↓ 1 new message" pill appears in the bottom-right; click it → smooth-scrolls to bottom, pill disappears; (2) scroll up without anyone sending — a subtle white circle with a down arrow appears in the same position; click → smooth-scrolls; (3) open a conversation with 5 unread messages — instead of landing at the bottom, the view scrolls to a "New messages" divider with the first unread message just below it; refreshing doesn't re-insert the divider (it's a one-time orientation aid for that session). **Known limitations / open items:** (a) Pill/button positioning is `position:absolute; right:18px; bottom:96px;` — assumes the messages-pane's positioned ancestor has at least 96px of bottom padding (composer area). All 3 surfaces have a composer below messages so this works; for any future read-only screen where there's no composer, the controls would sit too low and need a different bottom offset. (b) Unread divider stays in place if the user doesn't refresh/navigate — once they've seen the new messages, the next opening of that conversation will render without it (since unread is now 0). Intentional. (c) Pill is per-page state (in-memory); navigating away and back resets the count. That's the right behaviour because the next open re-renders fresh. (d) `ConvScroll.install` walks up looking for a positioned ancestor and sets `position:relative` on it if static. For all 3 surfaces the chat columns are already positioned, so no visible side-effect. |
| v74l | **Chat scroll-jump fix across all three conversation surfaces.** Scrolling up to read older messages, the 5-second refresh poll yanked the view back to bottom — happened on customer (`conversations.html`), admin (`admin-conversations.html`), and partner (`pp-conversations.html`). **Root cause:** `pinToBottom(wrap, { observe: false })` in each `refreshMessages` path was unconditional. By the time it ran, the renderer had already done `wrap.innerHTML = ''` + re-built messages, so `scrollTop` was already 0 — couldn't measure where the user actually was inside `pinToBottom` itself. **Fix in 4 places:** (1) `assets/conv-scroll.js` now exposes a new `window.wasAtBottom(wrap)` helper (threshold: within 80px of the bottom edge counts as "at bottom" — enough buffer for live-typing UX without snapping back a deliberately-scrolled user). `pinToBottom` gets a new `force: boolean` option; when `observe: false` and `force !== true`, the pin is skipped entirely. (2)(3)(4) The three `refreshMessages` functions now sample `wasAtBottom(wrap)` BEFORE re-rendering, then pass it as `force` to `pinToBottom`. **Behaviour after fix:** if user was at bottom, refresh still pins them (new messages appear and stay visible). If user scrolled up to read history, refresh leaves them where they are — they can scroll down themselves when ready. **Initial conversation-open and after-send pin calls are unchanged** — those use `observe: true` and always pin (correct: it's a freshly-opened thread or the user just hit send and wants to see it land). **Verify after deploy:** (1) open any conversation with 20+ messages, scroll up to read history, wait 6-10 seconds — view stays where you scrolled to (no jump); (2) scroll back to within ~80px of bottom, wait for the next poll — pin re-engages (back-to-bottom expectation preserved when user is following live); (3) send a message — appears at bottom, view pins to it. Same behaviour across customer / admin / partner. **Process note:** the v74l API uses an externalised `wasAtBottom()` call rather than internalising scroll detection in `pinToBottom`. That's because by the time `pinToBottom` runs, the renderer has already wiped `innerHTML` — `scrollTop` is no longer meaningful. The caller is the only one positioned to capture state at the right moment. |
| v74k | **Amendment payment switched to inline Payment Element + "Invalid Date" fix.** Two issues from real-world v74j testing. **(1) "Invalid Date" rendered above and below amendment cards in the conversation.** The conversation message renderer reads `m.sentAt`, but my v74j amendment cards wrote only `m.timestamp`. `new Date(undefined).toLocaleTimeString()` returns the string "Invalid Date" — hence the literal text in the UI. Fixed: all four amendment-card message pushes (amendment_card, amendment_accepted, amendment_declined in `postAmendmentCardToConversation`, plus the webhook's amendment_accepted system card) now write BOTH `sentAt` AND `timestamp`. **(2) Amendment payment now inline, not Stripe Checkout redirect.** v74j's `/api/amendment/checkout` created a Stripe Checkout Session and redirected the guest off-site — inconsistent with the deposit-payment UX which uses an embedded Payment Element via `TBStripeInline.payDeposit()`. **Worker changes:** (a) deleted `/api/amendment/checkout`; (b) added `POST /api/amendment/create-intent` mirroring `/api/checkout/create-intent` — creates a Stripe PaymentIntent, returns `client_secret`+`publishable_key`, sets `inlineFlow: 'true'` and `amendmentId` in PI metadata; (c) extracted the 200-line amendment-confirm logic from the Stripe webhook into a shared module-level helper `confirmAmendmentPayment(env, params)` (idempotent via `booking.stripeAmendmentSessions[]`); (d) webhook amendment branch now calls the helper in a single line; (e) `/api/checkout/sync-payment` extended with an amendment branch — if PI metadata has `amendmentId`, runs auth check then calls the shared helper. Whichever fires first (webhook or sync) wins via idempotency. **Client changes:** (a) new `window.TBStripeInline.payAmendment({ amendmentId, amendmentLabel, onSuccess, onCancel, onError })` in `assets/stripe-inline.js` — mirrors `payDeposit` but calls amendment endpoints and stays on the conversation; (b) `handleAmendmentDecision` updated — opens the inline modal instead of redirecting; on success, refreshes conversation so amendment_accepted card appears; (c) confirm-dialog copy reworded ("A secure payment form will open here" rather than "You'll be taken to a secure Stripe checkout"). **Architecture note:** the helper-extraction is the right call. Both webhook and sync-payment now share a single source of truth for "what to do when an amendment payment lands" — future bugs only need fixing in one place. Same pattern the deposit-payment flow already uses. **Verify after deploy:** (1) on a confirmed booking, partner creates an amendment with delta > $0 — the previously-doubled "Invalid Date" labels above/below the amendment card should now show real times like "11:42 AM"; (2) customer clicks "Approve & pay $X" — branded tbConfirm appears, click Continue — the inline TBStripeInline modal opens (NOT a Stripe-hosted page redirect); (3) pay with test card 4242 4242 4242 4242 — modal shows "Confirming your booking change…" → success → conversation refreshes — amendment_accepted card appears, booking row total updates, customer stays on `/conversations.html` throughout. **Known limitations:** confirm dialog re-confirmation step is a little redundant now that the inline modal also has its own Cancel button — could remove the tbConfirm pre-step in a follow-up. The `return_url` `?amendment=success` is mostly cosmetic since the inline flow handles non-3DS confirmation client-side; only matters for cards requiring 3DS where Stripe redirects through their auth flow before returning. |
| v74j | **Amendments Build 2 \u2014 real Stripe delta-charge wiring + 3 modal/data bug fixes.** Largest amendment-flow build of the v74 series. Replaces the v74e \"Build 1 stub\" with the actual Stripe commission-collection flow: guest approves an upgrade → routed to Stripe Checkout for the commission delta → webhook confirms → booking updates atomically → property settles remaining balance with guest separately (same model as initial offers, just on the delta). **(1) Guest count pre-fill bug.** Bookings store `guests` as a string (sometimes `"2 adults"`, sometimes `""`, sometimes `"2"`). The v74e `b.guests || 1` fallback worked for empty strings but assigning `"2 adults"` to a `<input type=\"number\">` left it blank. Fixed: `parseInt(b.guests, 10) || 0` then `|| 1` for the default, robust against all stored formats. **(2) Doubled room fields \u2014 fixed structurally, not by patching symptoms.** This was the 4th attempt at this bug; the previous fixes treated visible side-effects without solving the architectural issue. Two fundamental changes: (a) `openAmendmentModal` now aggressively resets ALL UI state on open (wipes `menu.innerHTML`, force-hides custom input, clears hidden values, closes any open dropdown) so no state can leak across opens \u2014 critical fix because the previous bug where \"Luxury Room\" appeared on properties without that room was caused by stale menu HTML from a previous open. (b) `amWireRoomDropdown` rewritten with append-if-missing strategy: when the booking's current room isn't in the property's `rooms[]`, it's prepended to the dropdown as a normal option (labeled \"current booking\") rather than triggering custom-mode. Custom mode is now ONLY entered when the partner explicitly clicks \"Other / custom\u2026\". The structural guarantee: the custom input is never visible unless explicitly chosen. Also added stale-fetch guard: if user opens booking B while booking A's `/api/property` fetch is in flight, the late response is ignored. **(3) Branded modal helper.** New `tbConfirm({title, body, primaryLabel, primaryStyle, secondaryLabel})` and `tbAlert({title, body})` helpers in `conversations.html` replace native `confirm()` / `alert()` for amendment dialogs. Cream/ink/terracotta palette matching the chat surface, fade+slide animations, Esc/Enter keyboard support, backdrop click closes appropriately, danger-style red button for destructive actions. Used for: approve confirmation, decline confirmation, error dialogs in `handleAmendmentDecision`, and the new amendment-return banner. **Scope note:** other call sites in `conversations.html` (alert/confirm for offer decline, payment errors, sign-in checks) still use native dialogs \u2014 those should migrate in a follow-up sweep but were out of scope for this build to keep it tight. **(4) The big one: Stripe delta-charge integration.** The v74e model was fundamentally wrong \u2014 it asked the property to invoice the guest for the additional deposit manually. Correct model (matching the existing initial-offer flow): **guest pays The Bearing the commission delta inline via Stripe, property collects remaining balance directly.** For a $1000 upgrade at 15% commission: $150 goes to The Bearing via Stripe immediately, property settles the remaining $850 with the guest using their normal flow. **Worker changes:** (a) New endpoint `POST /api/amendment/checkout` creates a Stripe Checkout session for the commission delta (`unit_amount = delta_deposit * 100` cents), metadata keyed on `amendmentId` (distinct from `offerId` so webhook can route correctly). Success URL returns to `/conversations.html?id=X&amendment=success`. (b) Stripe webhook handler extended with an amendment-payment branch that runs BEFORE the existing offerId branch: idempotent via `booking.stripeAmendmentSessions[]` index, flips amendment to `accepted`, marks original offer `superseded_by_amendment`, updates booking effective state (room/dates/guests/total/deposit), adds the paid commission to `booking.depositPaidAmount`, posts `amendment_accepted` system card with payment-confirmation text, sends branded internal email (action: property collects balance) and guest email (confirmation + balance settlement note). (c) Commission lookup completely rewritten. The v74e code used `commission_pct ?? 0.30` with formula `total * commission_pct` \u2014 wrong in BOTH units (commission_pct is 1-25 representing 1%-25%, not a decimal fraction) and magnitude (0.30 fallback would have given $0.30 instead of $300 on a $1000 booking). Fixed: read `commission_pct_at_time` from the confirmed offer (`booking.confirmed_offer_id`) preferentially, fall back to property's current `commission_pct`, with formula `Math.round(total * pct) / 100`. Returns 400 with code `NO_COMMISSION_PCT` if not found anywhere (data integrity issue). (d) PATCH `/api/amendment` approve endpoint now rejects amendments with `delta_total > 0` with `PAYMENT_REQUIRED` error \u2014 prevents bypassing payment by directly hitting the PATCH endpoint. PATCH approve is now exclusively for delta=0 amendments (price-neutral changes like same-priced room swap). **Client changes:** (e) `handleAmendmentDecision(amendmentId, action, approveBtn, declineBtn, deltaDeposit)` rewritten: for `action='approve'` with `deltaDeposit > 0`, POSTs to `/api/amendment/checkout`, gets a Stripe URL, redirects (window.location.href). For delta=0 approve and all declines, uses existing PATCH flow. All error paths use `tbAlert` instead of native `alert()`. (f) `openAmendmentModal` now caches the property's `commission_pct` on `amendmentState.commissionPct` so `amRecalc()` can compute the deposit delta correctly client-side; before commission is loaded, shows \"Calculating commission split\u2026\" rather than a wrong number. (g) Partner-side delta preview now shows the actual money split: \"Guest pays $150 to The Bearing inline (commission). You collect the remaining $850 from the guest directly.\" (h) Customer-side `amendment_card` button labels match the real flow: \"Approve & pay $150\" + explanatory note explaining the commission-vs-balance split. (i) New `handleAmendmentReturn` runs on conversations.html load: detects `?amendment=success|cancelled` query param, shows branded `tbAlert` explaining the outcome, then strips the param so refresh doesn't re-trigger. **Architecture notes:** amendments are still stored as offer records (sharing `offer:*` KV namespace) with extra fields. The webhook's amendment branch is fully self-contained (doesn't depend on the endpoint's helper functions being in scope); system-card posting is inlined to keep it scope-free. **Verify after deploy:** (1) on a confirmed booking in pp-bookings, click Amend \u2014 guest count is pre-populated, room dropdown shows ONE field with correct current room selected, total is correct; (2) edit room to upgrade, see live delta preview with \"Guest pays $X to The Bearing inline, you collect $Y from guest\" framing; (3) submit \u2014 no \"Network error\", row gets pending pill; (4) as guest, open the email link, sign in (lands on the conversation), see the amendment_card with \"Approve & pay $X\" button; (5) click Approve \u2014 branded confirm modal explains the Stripe flow, click Continue, lands on Stripe Checkout; (6) complete payment with Stripe test card (4242 4242 4242 4242) \u2014 returns to conversation with branded success banner, amendment_accepted card appears, booking updates on /bookings page; (7) check guest email confirmation \u2014 mentions balance settlement; (8) check admin email \u2014 mentions action to collect balance from guest; (9) try cancelling payment in Stripe \u2014 returns with branded cancelled banner, amendment still pending (re-clickable); (10) for a price-neutral edit (no price change), PATCH path still works (no Stripe redirect). **Known limitations / follow-ups:** (a) admin-side and pp-side amendment_card renderers are unchanged (still display read-only); they don't need the new flow because partners and admin don't approve their own amendments. (b) Other native confirm/alert calls in conversations.html (offer decline, payment errors) still use browser chrome \u2014 should migrate to tbConfirm/tbAlert in a follow-up sweep. (c) The Stripe webhook subscription must include `checkout.session.completed` AND `payment_intent.succeeded` for both initial-offer and amendment-payment flows \u2014 if either is missing, payments will succeed silently without the booking updating. Verify the subscription in Stripe Dashboard. (d) Refunds for cancelled amendments are NOT in scope here; if a guest disputes a Stripe charge after the amendment was applied, the booking state will still reflect the amendment until manually rolled back. Build 3 (downgrades + refunds, deferred per spec) will address. |
| v74i | **Four bugs from real-data amendment testing.** **(1) "Network error" on send despite the email going through.** Root cause: `submitAmendment`'s success path called `toast()` but the actual function on this page is `ppToast()`. The ReferenceError was caught by the catch-all `.catch` and surfaced as "Network error" even though the worker succeeded. Fixed: now calls `ppToast()`, wrapped in try/catch so future helper-name drift doesn't surface as a network error. Also refactored response handling to read as text first then parse JSON, so an empty body or HTML response doesn't trip the same catch-all (`r.json()` throwing inside the chain). **(2) Emails used the slug instead of the property name.** Subject lines and body copy said "gypsy-by-mekong-kingdoms" instead of "Gypsy by Mekong Kingdoms." Root cause: same field-naming-mismatch class as v74g's `confirmed_total_amount`. Bookings store the readable property name as `booking.property` (set at line 1915 when bookings are created). The v74e amendment endpoint read `booking.propertyName` which is undefined on bookings (it's a field on offers and conversations, not bookings). Fixed in 3 places: amendment record creation, guest proposal email, and admin+partner approval email. **(3) Login redirect dropped to homepage instead of the conversation.** Guest opens the amendment email on a logged-out device → clicks CTA → arrives at `/conversations.html?id=X` → sign-in overlay → after sign-in, dumped on `/my-account.html`. Root cause: `Clerk.mountSignIn()` in `conversations.html` was hardcoded with `afterSignInUrl: '/my-account.html'`. Fixed: now uses `window.location.href` (preserves the `?id=X` query string), so guest lands back on the exact conversation they came from. **(4) "Approve & pay $X" button doesn't actually charge.** This isn't a regression \u2014 it's the deliberate Build 1 stub. Build 1 is data model + UI only; Stripe delta-charge is Build 2. But the button label "Approve & pay $X" promised payment that doesn't happen, which is misleading. Changed copy: button now reads "Approve change \u00b7 $X to settle" (delta amount as a sublabel, not a "pay" promise). The note below the buttons spells out the actual flow: "Approving will record the change. The property will then email you a separate payment link for the additional $X deposit \u2014 you don\u2019t pay directly through this page yet." Added a confirm dialog before submitting that re-states the same point so guests aren't surprised by the missing Stripe redirect. This is a stopgap copy fix \u2014 the real solution is shipping **Build 2 (Stripe delta-charge via payment_intent.succeeded webhook)** next, which is its own focused session. **Verify after deploy:** (1) submit an amendment as partner \u2014 see success toast, no "Network error" message, booking row picks up pending pill; (2) check the email subject \u2014 "Proposed change to your Gypsy by Mekong Kingdoms booking" not the slug; (3) open the email link in a logged-out browser, sign in, land on the same conversation (not `/my-account`); (4) click "Approve change \u00b7 $X to settle" \u2014 confirm dialog explains the separate payment link flow, click OK, amendment is accepted, booking updates, no Stripe redirect (intentional until Build 2). |
| v74h | **Amendment modal: structured change preview + error block redesign.** Two UX issues from post-v74g testing. **(1) Change preview was a flat label list** — "Changing: room, departure date, guest count, price" told the partner WHICH fields changed but not WHAT they were changing TO. Replaced with a structured before-vs-after block: only fields that actually changed get a row (Room, Dates, Guests), each showing `[OLD] → [NEW]` with the old value struck-through and the new value bolded. Mirrors how the guest sees the amendment_card after submission — partner now previews exactly what the guest will see. Dates row combines arrival+departure into one entry. Price-delta block stays separate (Price impact section) so the money question is visually distinct from the field changes. If a field IS changed but the price is unchanged (e.g. same-priced room swap, party reshuffle), the price block still shows "Price unchanged — no additional charge" for completeness. **(2) Error block overlapped the textarea above it.** Looked like a continuation of the modal body instead of a distinct alert. **Fixes:** added an error icon (circle-with-exclamation SVG), inline padding bumped to 14px/22px, switched parent to `display:flex` so icon + text align cleanly, kept the bottom border separator from the footer below. Inner text now writes to a dedicated `am-error-text` element so `textContent` updates don't wipe out the icon sibling. Same scroll-into-view behavior preserved so the error still scrolls itself into view from anywhere in the modal body. **Verify after deploy:** (1) Amend a booking, change room + dates + total — preview shows the new structured "What's changing" rows with old→new for each, plus a separate "Price impact" block; (2) try submitting with guests field empty — error appears above the Send button as a clearly-bordered alert with an icon, not a stripe that overlaps the textarea; (3) when only price changes (same room, same dates, same guests) — "What's changing" block doesn't render, only the Price impact block; (4) when only a non-price field changes — "What's changing" renders the row, Price impact shows "Price unchanged." |
| v74g | **Amendment modal — three real bugs found by Miguel in post-v74f testing.** **(1) Original Total showed $0 on every confirmed booking.** Root cause: confirmed bookings store their canonical totals as `confirmed_total_amount` (set by the Stripe webhook on successful payment), with `depositPaidAmount` for the deposit. The v74e/v74f amendment code read `b.totalAmount || b.total_amount` which don't exist on confirmed records → always fell through to `0`. **Fixed in 3 places:** (a) `openAmendmentModal` pre-fill uses `confirmed_total_amount || totalAmount || total_amount` fallback chain; (b) `amRecalc` uses same chain for delta computation; (c) worker `previous_state` extraction in `POST /api/amendment` uses same chain so the amendment record's previous_state snapshot has correct numbers. Also fixed worker's approval handler: when an amendment is accepted, the worker now writes BOTH `confirmed_total_amount` AND `total_amount` on the booking — otherwise the bookings UI (which reads `confirmed_total_amount`) would keep showing the old total. `depositPaidAmount` is intentionally NOT overwritten on approval — it represents what was actually paid through Stripe; the delta will be invoiced manually until Build 2. **(2) Literal `\u2014` (and `\u2026`, `\u00b7`, `\u2192`) rendering as visible text in the modal.** Root cause: the v74f session wrote Unicode escape sequences in raw HTML and HTML attribute values, where they're not parsed as escapes. JavaScript parses `\u2014` inside `'...'` string literals to em-dash, but HTML doesn't — `<div>\u2014</div>` renders the literal text `\u2014`. **Fix:** identified all literal-in-HTML occurrences (4 in `pp-bookings.html` — placeholder attributes and div text content), replaced with actual Unicode characters. Wrote a Python pass that distinguishes JS-string-context (safe) from HTML-context (broken). Total of 4 hand-fixed + 6 automated replacements. Other modified files (`conversations.html`, `admin-conversations.html`, `pp-conversations.html`, `bookings.html`) were audited but their `\u` sequences are all inside JS string literals → fine. **(3) Room dropdown doubled — both a select and a text field visible.** Root cause: when the booking's `b.room` value didn't match a property's `rooms[].name` exactly (case-insensitive but no trim), the initial-setup logic entered "custom mode" and rendered the text input below the dropdown. Plus, when the user later clicked a matching room from the dropdown, the old `amSetRoomDropdownValue()` didn't hide the custom text input from the previous custom-mode state. **Fix:** (a) `amSetRoomDropdownValue()` now explicitly hides + clears the custom input and resets `amendmentState.customMode = false` whenever it's called; (b) initial match uses `trim().toLowerCase()` on both sides so " Luxury Room " from an offer-builder dropdown matches "Luxury Room" from the property; (c) when a match IS found, the property's canonical room name is used (preserves casing/spacing exactly). **Worth flagging for follow-up:** the offer-builder dropdown that creates these bookings should also trim values on write. If you create new bookings via the offer modal and they later look funny in the amendment modal, that's likely upstream — happy to fix that separately. **Verify after deploy:** (1) open Amend on any confirmed booking — Original Total card shows real numbers, not $0; (2) all em-dashes, ellipses, and dots render properly throughout the modal; (3) Room dropdown shows ONE selector with the booking's current room pre-selected, no second text field below it (unless the booking's room genuinely isn't in the property's rooms list, in which case custom-mode is expected). |
| v74f | **Amendment modal UX overhaul + "not authorized" 403 bug fix.** Post-v74e testing surfaced five concrete issues, all addressed. **(1) Worker `not authorized to amend this booking` 403 — root cause found.** Bookings store the property slug as `booking.slug`, NOT `booking.propertySlug` (verified via grep: line 1356 of envoy.js, the booking-list filter, uses `b.slug`). The amendment endpoint's trust check was comparing `slugClaim === booking.propertySlug` which is always undefined on real bookings — so `slugMatches` was always false and every partner POST got 403'd. Fix: worker now reads `booking.slug || booking.propertySlug || ''` and compares against that. Client side: `submitAmendment` now sends `b.slug || b.propertySlug || PP_SLUG`. Same fix applied to all other `booking.propertySlug` reads in the amendment endpoint (amendment record creation, propertyName resolution, partnerEmail fallback). **(2) Room dropdown instead of free-text.** Modal had a free-text room input — easy to typo, no awareness of what the property actually offers. Now uses a `tb-select` dropdown (parallel to the offer modal's `om-room-*` dropdown but prefixed `am-room-*` to avoid ID collisions). Populated from `/api/property?slug=X` returning `prop.rooms[]`. Custom rooms still supported via "Other / custom…" option that swaps to a text input — handles the case where a partner needs to amend to a one-off package name. Reuses existing `.tb-select-*` CSS so styling is consistent with the offer modal. **(3) Original vs new total side-by-side.** The "new total" label was ambiguous — partner couldn't tell whether to enter the additional amount or the full new total. Now the modal shows a card with original total + already-paid deposit on the left (read-only) and a "New total for whole booking" input on the right (filled with current total as the starting value), with explicit helper text "Enter the full new total — not the additional amount." **(4) Delta phrasing rewrite.** Was: "+ $X total · + $Y additional deposit due". Now leads with the **difference** explicitly: "+ $X difference ($oldTotal → $newTotal)" then "Guest needs to pay an additional $Y deposit (deposit goes from $oldDeposit → $newDeposit)". Much clearer what's happening. **(5) Error display in footer above submit button.** The validation/server error block was at the TOP of the scrollable modal body — invisible when the partner clicked Submit at the bottom of a tall modal. Moved to the footer above the submit button so it's always visible. Also rewrote validation messages to be specific: was "All trip fields are required" (you had to figure out which one); now "Please fill in: arrival date, guest count" — names the exact missing fields. Server errors (including the now-fixed 403 case) also surface in this footer block. **Additional small touches:** open-modal now shows an "Original booking" summary card at the top with the room/dates/guests for reference; "New total" input pre-fills with the current total (not 0) so partner just edits up rather than retyping the whole number. **Why this matters:** the v74e shipped functionally complete but the UX friction made it scary to actually use. v74f makes the modal behave like a real production form — clear labels, dropdown for known values, errors that you can see when you make them. **Verify after deploy:** (1) on a confirmed booking, click Amend → dropdown shows the property's room list + "Other / custom…" option; (2) clicking the property's actual rooms or selecting custom and typing both work; (3) the Original total / New total card makes it obvious which is which, with the new total pre-filled at the current value; (4) try clicking Send without entering anything → error appears in the footer, lists missing fields by name; (5) the previous 403 should be gone — amendment actually sends and shows up as a card in the conversation. |
| v74e | **Booking Amendments — Build 1 (data model + UI, no Stripe yet).** Post-payment changes to bookings now flow through the platform instead of out-of-band email between partner, admin, and Stripe. Largest feature build of the v74 series — ~1700 lines added across 5 files. **(1) Worker: new `/api/amendment` endpoint with POST (partner creates) and PATCH (guest approves/declines).** Trust model: POST is admin OR partner-by-slug (matches pp-* pattern); PATCH is ownership-by-email against `booking.email`. Amendment records are stored as offer records (`offer:offer_am_*`) with extra fields `amendment_of`, `amendment_kind` (upgrade/date_change/duration_change/party_change/mixed), `delta_total`, `delta_deposit`, `previous_state` (snapshot). Auto-detects `amendment_kind` from what changed. Validates: only deposit_paid non-cancelled bookings can be amended; `delta_total < 0` returns 400 with `DOWNGRADE_NOT_SUPPORTED` (downgrades land in Build 3 once a refund policy is written). On POST: writes amendment offer, appends to `__offers_by_booking:{ref}` index, sets `booking.pending_amendment_id`, posts `amendment_card` system message into the linked conversation, emails guest with branded shell + before/after diff. On PATCH approve (Build 1 stub): marks amendment `accepted`, marks original offer `superseded_by_amendment` (new lifecycle state), updates booking record's effective state (room/arrival/departure/guests/total_amount/deposit_amount), pushes amendment id onto `booking.amendments[]`, clears `pending_amendment_id`, posts `amendment_accepted` system card, emails admin+partner with **"Build 1 stub: please manually invoice $X delta"** instruction (Stripe delta-charge replaces this in Build 2). On PATCH decline: marks amendment `declined`, clears pending pointer, posts `amendment_declined` system card. `backfillBookingShape` extended to default `amendments: []` and `pending_amendment_id: null` so the partner UI can render decisions without checking field existence. **(2) Partner UI on `pp-bookings.html`:** new "Amend" button on confirmed (deposit_paid) booking rows, hidden while a pending amendment is in flight. Opens a slide-out modal pre-filled with current booking state. Live `amRecalc()` updates a delta preview as the partner edits — shows changing fields, new total, new deposit, additional deposit due. Negative deltas show inline red error blocking submit (partner is told to use loop-in to admin). Partner adds a free-form note. Submit calls `POST /api/amendment` and refreshes the list — row picks up "Amendment pending" terracotta pill. **(3) Conversation card rendering in all 3 conv UIs.** Three new system message types: `amendment_card` (proposal), `amendment_accepted` (confirmation), `amendment_declined` (decline). Customer side (`conversations.html`) renders the proposal card with functioning Approve & Pay $X / Decline buttons; on click, `handleAmendmentDecision()` PATCHes `/api/amendment` with the Clerk-supplied email and reloads messages. Admin + partner sides (`admin-conversations.html`, `pp-conversations.html`) render the same card read-only — admin sees "Proposed by partner · Awaiting guest response", partner sees "You proposed this change · Waiting on guest." All three system message types styled symmetrically across the three UIs: amendment_card uses terracotta-gradient (matches the loop-in visual language), amendment_accepted is moss-green understated (booking already existed; less celebratory than `booking_confirmed`), amendment_declined is muted stone (mirrors `offer_declined` treatment from v73at). **(4) Customer `bookings.html`:** confirmed rows with `pending_amendment_id` show a "✎ Change proposed — awaiting your response" terracotta pill in the meta row, so guests can find the amendment from the bookings page without digging into conversations. **(5) Three new branded transactional emails.** Guest on proposal: `Proposed change to your {Property} booking · {Ref}` — before/after table, delta highlighted, CTA back to the conversation. Guest on approval: `Booking updated · {Property} · {Ref}` — new state summary, note that property will invoice the delta separately. Admin+partner on approval: `[CONFIRMED] Amendment · {Property} · {Ref}` — internal tone with the **manual-invoice action-required block** that goes away in Build 2. **What's NOT in this build (intentional, per spec):** Stripe delta-charge wiring is the entire scope of Build 2 (will hook `payment_intent.succeeded` webhook to replace the manual-invoice email line). Downgrades + refunds are Build 3 (deferred until refund policy is written). Guest-initiated amendments are out of scope — guests message the partner, partner formalises. **One amendment at a time per booking** is enforced by `pending_amendment_id` — the Amend button hides while one is in flight. **Architecture note:** amendments are stored as offer records (sharing `offer:` KV namespace) rather than getting their own type. Rationale: they share most fields with offers, fit the existing per-booking offers index, and inherit the offer status lifecycle. The distinguishing fields (`amendment_of`, `amendment_kind`, etc.) gate amendment-specific behavior in the worker. Trade-off: the `/api/offer` listing endpoint now returns amendment records too — UI filters where needed. **Verify after deploy:** (1) on a confirmed booking in pp-bookings, click "Amend"; (2) edit room or total, see delta preview; (3) submit — row gets pending pill, Amend button disappears; (4) check guest's `conversations.html` — amendment_card with Approve & Decline buttons; (5) Approve as guest — buttons collapse, amendment_accepted card appears, booking row total/room updates in `bookings.html`, admin email arrives with manual-invoice instruction. **Limitations:** (a) commission_pct fallback of 30% when missing from booking — most bookings should have it set; if you see deposits look off, that's the fallback firing; (b) emails to partners use a placeholder `partners-{slug}@thebearing.io` if booking has no `partnerEmail` field — most workflows pass through admin notifications already; (c) bookings.html pending pill is non-clickable info only; user has to navigate to conversations themselves. **Also fixed in this build:** removed stale `property_corrupted.html` debug artifact left over from the v74d session. |
| v74d | **Demo property pages deleted + Book Now buttons removed site-wide + pp-settings honest "Coming soon".** Two cleanup tasks combined into one build. **(1) 12 demo property pages deleted entirely** (`amangiri.html`, `alila-jabal-akhdar.html`, `mekong-navigator.html`, `singita-grumeti.html`, `capella-ubud.html`, `al-moudira.html`, `al-moudira-estate.html`, `amangiri-villas.html`, `aqua-nera.html`, `beyond-ngorongoro.html`, `la-maison-bleue.html`, `soneva-fushi.html`). These were "abandoned scaffolding" per project context, but were still being linked from listing pages (8 cards on hotels.html, 3 cards on villas.html), causing the link targets to 404. Real properties now flow through `property.html?slug=X` (the canonical KV-backed template). **(2) `tbOpenCheckout` / `tbAuthBook` removed from public site.** Instant booking isn't a real flow yet (no Stripe wiring on the public side). HANDOFF claimed these were "superseded by v73" but they were actually still wired to every "Book now" button site-wide. Miguel confirmed the intent was to hide them until real instant booking arrives. **Scope of cleanup:** (a) **listing pages** — `hotels.html` lost 8 demo cards + the Book Now button on the Nour El Nil featured card, gained "More properties coming soon" empty state that shows when zero dynamic-KV hotels render; `villas.html` lost all 3 demo cards, gained same empty state; `cruises.html` lost Book Now from both the Nour El Nil voyage card and the dynamic loader; `HARDCODED` slug arrays trimmed; (b) **discovery pages** — `search.html` lost 3 Book Now buttons, `collections.html` lost 6, `vibe-search.html` and `index.html` and `the-bearing.html` had no buttons but did have the function definitions and the `tb-checkout-overlay` modal markup; (c) **modal cleanup** — the full `<div id="tb-checkout-overlay">…</div>` block (~20KB) plus the `<script>` containing `tbCheckout` state + `tbOpenCheckout` / `tbAuthBook` / `tbSubmitBooking` / `tbGoStep` functions (~10KB) deleted from 9 files (`property.html` + 8 others); (d) `@keyframes tbSlideUp` + `#tb-checkout-sheet` mobile CSS rules deleted. **Total: ~30KB removed from 8 files, ~130KB removed from property.html** (its script block was larger; required surgical extraction since property hydration code lived in the same `<script>` tag). **Flagship `nour-el-nil.html` left fully intact** — has its own Book Now buttons, tbOpenCheckout, tbAuthBook, tb-co overlay — all preserved since instant booking will return there first when real Stripe wiring lands. **(3) `pp-settings.html` — honest "Coming soon" body.** Replaced the empty stub with a clean coming-soon card. Removed two dead buttons that previously did nothing (the "+ Add new" topbar button and the "Get started" CTA). Added a link to `pp-notifications` since that's the one settings-adjacent feature partners can actually use today. **Out of scope but flagged:** the `CI_PROPERTIES` object in `index.html` / `nour-el-nil.html` / `the-bearing.html` (Envoy chat property-detection cards) still lists all 12 demo slugs. Cards link via `/property.html?slug=X` which won't break — but those property pages have no KV data, so a click leads to an empty property page. That's the same brokenness that existed pre-v74d; cleanup deferred. Also the `KNOWN_PROPERTY_SLUGS` and SPA-fallback page arrays still contain demo slugs — cosmetic, no functional impact. **Verify after deploy:** (1) `/hotels` shows only the Nour El Nil featured card + any KV-stored hotels (none currently) + the "More properties coming soon" empty state; (2) `/villas` shows the empty state (no KV villas yet); (3) no "Book now" buttons anywhere on the public site except inside `nour-el-nil.html`; (4) `/pp-settings` shows a clean Coming soon card with a link to `/pp-notifications`. **Process learning:** the per-file `<script>` boundary differs between pages — most listing pages had checkout in its own script tag, but `property.html` mixed checkout into the same script as `loadProperty()` / `renderProperty()`. The naive "delete the whole script tag" approach broke property.html on first pass; reverted and did surgical extraction using `/* checkout engine */` comment + `// ── KV HYDRATION ──` comment as anchors. Future cleanups of this kind should map per-file script structure before automating. |
| v74c | **Conv-list loop indicator (server-side embed) + Mark-Resolved overlap fix.** Two pieces. **(1) Loop indicator on the conv-list, not just inside the open conv.** v74a/b only painted the "🔒 N" pill via `flagSidebarLoopActive()` which runs from `pollAdminLoop()`, which only starts AFTER admin opens a conversation. Admin had a working sidebar nav badge ("you have unread!") but no way to find WHICH conv was calling for help — they had to click into each one to discover it. Miguel correctly flagged this as a critical UX gap. **Fix:** `/api/conversation` GET (both admin-list and partner-list branches) now embeds `loopSummary: { active, unreadAdmin, unreadPartner }` on each conv item when a loop record exists. `buildConvItem()` in `admin-conversations.html` + `pp-conversations.html` renders a `🔒 N` pill in the existing pills row (alongside unread / wait-time / archived pills). Admin uses `loopSummary.unreadAdmin`, partner uses `loopSummary.unreadPartner`. Pill omitted when no `loopSummary` field. **`flagSidebarLoopActive()` rewritten** — now updates the in-memory `conversations` model and re-renders the list via `renderList()`, instead of DOM-injecting a pill. The optimistic-update behavior is preserved (admin marks resolved → pill goes away immediately, without waiting for the next list refresh). Dead `.loop-active-pill` CSS removed. New CSS rule: `.conv-pill.loop-pill { background: rgba(176,88,48,.14); color: #b05830; border: 1px solid rgba(176,88,48,.22); }` added to both admin and pp conversations. **(2) Mark Resolved button no longer overlaps the loop-header title.** v74a placed the button as `position: absolute; top: 14px; right: 14px;`, which worked when the header was wide enough but covered the title text on narrower panels (Miguel screenshot showed "PRIVATE — PARTNER &" cut off by the button). **Fix:** new `.loop-header-top-row` flex container holds the title and the button side-by-side with `justify-content: space-between`. Title takes the remaining space (`flex: 1`); button auto-sizes with `flex-shrink: 0; white-space: nowrap`. **Pushback flagged:** the partner-side conv-list pill (`loopSummary.unreadPartner`) is also wired in this build — I'd previously pushed back on this in v74b, but since we're already doing the server-side embed for admin, partner is essentially free. Worth shipping symmetrically. **Verify after deploy:** (1) as admin in admin-conversations, before clicking into any conv, scan the list — any conv with an active partner loop should show `🔒` (or `🔒 N` if unread) in the pill row of the conv item; (2) click the 🔒 The Bearing tab + click Mark Resolved → button no longer overlaps the "Private — partner & admin" title text; (3) after Mark Resolved → the conv-list pill immediately disappears (optimistic update); (4) as partner, send a fresh loop message → admin sees the conv-item highlight up in the list with `🔒 1` without ever opening the conv. **Limitations:** the conv-list polls every 30s for fresh data (admin-conversations) / on visibilitychange (both). Initial pill appearance is instant on first load; subsequent updates lag up to 30s unless admin/partner actively opens the conv (which kicks the `pollAdminLoop`/`pollLoop` 12s cycle that calls `flagSidebarLoopActive` for immediate updates) |
| v74b | **Loop-in polish: unread badges everywhere, guest name in tab label, handshake pill replaced with a clean count badge.** Builds on v74a. **(1) Guest name in left tab.** Tab label was generic "Guest details" — now shows the guest's actual name truncated to 14 chars with ellipsis (e.g. "👤 Miguel Cancino"). Falls back to "Guest details" if no name (anonymous email-only enquiries). Applied in both `pp-conversations.html` and `admin-conversations.html`. **(2) Tab-to-tab badges both directions, both portals.** Each of the two tabs (`👤 [Name]` and `🔒 The Bearing`) now has its own unread-count badge that increments when new activity arrives on the OTHER tab. Clears when you click that tab. Implementation: on the loop tab side, badge count comes from `loop.unreadPartner` / `loop.unreadAdmin` on the server (survives page reloads). On the guest tab side, badge tracks the delta of new main-thread messages received while user is on the loop tab. Both fall to 0 on switch. **(3) Server-side `mark_read` action.** New `/api/loop` PATCH action discriminator: `{action: 'resolve'}` (admin-only, v74a default behavior preserved) or `{action: 'mark_read'}` (either side can call). Mark-read resets the caller's unread counter on the loop record. Fires automatically when user clicks their loop tab AND when their loop is actively visible during a poll that brought in new messages. **(4) Sidebar nav badge rolls in loop-unread.** `recomputeUnreadCounters()` now also scans `conversation:{id}:loop` for active loops and tracks `adminLoop` (count of convs with admin-unread loops) + `propsLoop` (per-slug partner-unread loop count). `/api/unread-count` returns these as `loopUnread` + a `hasLoop` boolean. Both `assets/admin-sidebar-badge.js` and `assets/pp-sidebar-badges.js` apply a `.sb-badge-has-loop` class when `hasLoop:true`, which renders the badge with a terracotta border + soft glow as a styling hint. The number itself is the *total* (main + loop) so the partner/admin sees one signal. **(5) `.sb-badge-has-loop` CSS rule added** to all 26 admin + partner pages via a sed-like rewrite next to the existing `.sb-badge{...}` rule. Style: `background:#b05830; box-shadow:0 0 0 2px rgba(176,88,48,.18)`. **(6) Handshake pill replaced with clean `🔒 N` count badge.** Admin-side conv-list-item indicator was "🤝 Partner needs help" — Miguel correctly flagged this as redundant with the "🔒 The Bearing" tab. Now shows `🔒` (if loop active, no new) or `🔒 3` (if loop has 3 unread admin messages). Count comes from `loop.unreadAdmin` and updates on every poll. **What's NOT in this build (intentional pushback):** partner-side conv-list-item indicator. Adding it would require either (a) N+1 API calls to fetch per-conv loop state or (b) extending `/api/conversation?slug=X` to embed loop summaries. Skipped because partner typically has only 1-2 active loops at a time and already sees activity via the sidebar badge + right-panel tab. Will add if Miguel actively misses it. **Verify after deploy:** (1) load pp-conversations, click into a conv → tab on left now shows guest's name truncated, not "Guest details"; (2) from another browser as admin, reply in the loop → partner side should show "1" badge on 🔒 The Bearing tab AND sidebar conv-count badge has terracotta border; (3) click the loop tab on partner side → badge clears, server marks read, sidebar badge drops; (4) while admin is on loop tab, have guest send a main-thread message → admin sees "1" badge appear on the `👤 [Name]` tab; (5) admin-conversations conv-list shows `🔒 N` badge on threads with active loop unread instead of the old handshake pill. **Limitations:** (a) tab badges only visible while you're already inside a specific conversation — they don't broadcast across conversations (use the sidebar badge for that); (b) the guest-tab delta-tracking is in-memory only — if you reload the page mid-poll while on the loop tab, the guest-tab badge starts at 0; the server-side mark_read on main-thread is unchanged so the underlying counters are still correct |
| v74a | **Loop-in: partner-to-admin private side-thread on a conversation.** Partners need a way to ask The Bearing for help on a specific guest conversation (e.g. "guest wants to upgrade, can you process the delta?") without that exchange being visible to the guest. **Versioning note:** rolled major from v73 to v74 because we hit `v73at` and the `aa`–`az` letter run was getting unwieldy. Build name format unchanged otherwise (`v74a`, `v74b`, ...). **Architecture:** the loop is a structurally separate KV thread at `conversation:{id}:loop` (record) + `conversation:{id}:loop:messages` (array). The customer's `/api/conversation` GET never returns loop data. Loop is read exclusively via the new `/api/loop` endpoint. The customer renderer in `conversations.html` doesn't know about and never queries these keys — making accidental leakage to guests structurally impossible. **New worker endpoint `/api/loop`:** GET (admin or partner-by-slug), POST (admin or partner-by-slug; creates loop on first call), PATCH (admin-only, marks resolved). First partner message fires `[LOOP-IN] {Property} · {Guest}` branded email to admin via `loadNotificationRecipients()`, with the partner's note + last 3 messages from the main thread as context. Subsequent messages don't trigger more emails (admin is already in the loop after the first). Loop auto-reactivates if partner posts a new message after admin marked it resolved. Email bypasses all notification mute settings — partner-initiated escalations are urgent. **Loop record fields:** `active`, `requestedAt`, `resolvedAt`, `reactivatedAt`, `partnerSlug`, `lastMessageAt`, `unreadAdmin`, `unreadPartner`. **UI — pp-conversations.html:** the right-hand context-panel (previously just guest details) now hosts two tabs — `[👤 Guest details]` + `[🔒 The Bearing]`. The loop tab shows the private chat panel with a clear "Private — The Bearing only" header indicator, terracotta-tinted message bubbles for partner outbound, white bubbles for admin replies, distinct diagonal-stripe background to differentiate from the main thread. Composer labeled "🔒 Private to The Bearing" with terracotta accent. Polls every 12s for admin replies; surfaces a `!` badge on the tab if admin replied while partner was on the guest tab. **UI — admin-conversations.html:** same tab structure on the right panel. Admin sees their own messages on the right (dark gradient bubbles matching the existing admin role styling) and partner messages on the left. Loop pane includes "Mark resolved" button (admin-only) in the loop header. Sidebar conv list items get a "🤝 Partner needs help" pill auto-injected on threads with active unresolved loops, scoped via `flagSidebarLoopActive(convId, isActive)` keyed off the existing `data-id` attribute on `.conv-item`. **UI — conversations.html (customer):** zero changes. The customer literally cannot see anything related to loops because the data lives in a separate KV key the customer renderer never reads. **Trust model:** partner auth on `/api/loop` GET/POST is trust-by-slug (slug claim in URL/body must match `conv.propertySlug`), consistent with the existing pattern for all `pp-*` endpoints. Admin auth uses standard `isAdmin()` via `X-Admin-Email` header. Real partner auth is still on the deferred work list. **Spec doc added:** `docs/AMENDMENTS_SPEC.md` — captures the design for post-payment booking amendments (upgrades, downgrades, date changes) including Stripe delta-charge model, lifecycle states, system message types, UI flow, and open questions. The loop-in feature is the manual escalation mechanism for amendments until that spec is built. **Verify after deploy:** (1) load `pp-conversations.html?as=nour-el-nil&id={someConvId}`, click the 🔒 The Bearing tab on the right panel, type a private message → check `admin@thebearing.io` inbox for `[LOOP-IN]` branded email; (2) load `admin-conversations.html?id={sameConvId}`, confirm the conv shows "🤝 Partner needs help" pill in sidebar AND the 🔒 The Bearing tab is highlighted with partner's message visible; (3) reply from admin side → poll back to partner side, verify partner sees admin's reply in their loop pane; (4) admin clicks "Mark resolved" → sidebar pill disappears, header status flips to "Resolved — new messages will reopen"; (5) partner sends a follow-up message → loop auto-reactivates, admin sees it again; (6) confirm `/api/conversation?id={id}` GET response does NOT contain any loop data — the customer renderer cannot accidentally surface it. **Known limitations:** (a) trust-by-slug isn't real auth; a malicious actor with knowledge of a property's slug could read/post loop messages — fine for current trust model, gated on real partner auth before public launch; (b) mobile responsive design not addressed — pp-conversations.html has no media queries today and the tab approach inherits that limitation; (c) no SVG icon for the loop tab; using emoji 🔒 — could swap for inline SVG later if cross-platform rendering becomes inconsistent |
| v73at | **Branded transactional email template + guest enquiry confirmation email (NEW) + offer_declined in-app card.** Three coordinated pieces. **(A) Email shell + migration:** new top-level helpers in `functions/api/envoy.js`: `escapeEmailHtml(str)`, `renderEmailShell(opts)` (cream `#faf7f1` body, Cormorant Garamond "THE BEARING" wordmark header with Georgia fallback for Gmail, terracotta `#b05830` accents, footer with admin@thebearing.io contact, preheader text for inbox preview, optional CTA + secondary text link + reference label + unsubscribe link), and `sendBrandedEmail({env, to, subject, replyTo, text, shell, from?, logTag})` that wraps `fetch('/emails')` and guarantees both HTML + plain-text. All CSS inlined; max-width 560px. 19 of 21 Resend sends now route through `sendBrandedEmail`. 15 of those 19 were previously plain-text only — now also have branded HTML. Remaining 2 raw fetches are intentional (test-email diagnostic surface; the helper itself). **(B) NEW email — guest enquiry confirmation:** fires inside `/api/conversation` POST action='create'. From: `The Bearing <bookings@thebearing.io>`, Reply-To: `reply+{convId}@replies.thebearing.io` so replies route back into the conversation via inbound webhook. Subject: "Your Enquiry for {Property Name} Was Received". Body: warm acknowledgment, optional "What you sent" summary (dates / party size / cabin if provided in `enquiry`), guest's message in a card, CTA to view conversation, footer note about reply routing. **Always sends regardless of `notifyAdmin`/`notifyGuest`** — it's a transactional receipt. `bookings@thebearing.io` set up as a Google Workspace alias on the admin user before shipping, so replies that ignore Reply-To still land visibly. **(C) replyTo fix on `Booking-Confirm-Guest` (Stripe webhook):** the partner version (`Booking-Confirm-Partner` at L5042) had `replyTo: convIdForPartner` but the guest version at L4955 didn't. 3-line fix: read `booking.conversationId` and set `replyTo: 'reply+' + convId + '@replies.thebearing.io'` when present. Now both halves of the deposit-confirmation pair route replies into the conv thread. **(D) `offer_declined` in-app card (3 conversation UIs):** when a guest declines a partner offer, worker posts a system message with `type: 'offer_declined'` (existed since v73as) but none of the three conv UIs had renderers for it — rendered as plain text. Added muted/somber card treatment in `conversations.html`, `admin-conversations.html`, `pp-conversations.html`: stone/bark palette (no red — declined is a setback, not a failure), back-arrow icon (not a checkmark or X), eyebrow "OFFER DECLINED", main text, optional quoted reason in italic block. Customer CSS uses `ch-` prefix + no `!important`; admin uses `.msg-bubble` + `!important` for dark chrome override; partner uses `.msg-bubble` + `!important` + `DM Sans` font family. Hover actions (emoji react / quote / copy) skipped on these cards, matching the existing `offer_card` + `booking_confirmed` pattern. Worker text format `"Guest declined the offer. Reason: ..."` is split on `" Reason: "` so the reason renders as a quoted block separately from the main text. **Verify after deploy:** (1) submit a test enquiry → guest inbox confirmation email, Reply routes to conv; (2) reply to confirmation from guest mailbox → message appears in admin-conversations thread; (3) admin + partner alerts on same enquiry render branded; (4) pay a deposit via Stripe → guest booking-confirmed email Reply also routes to conv; (5) decline an offer from the customer side → all 3 portals show the new muted card with reason quoted, NOT plain text. **Audit notes:** this build inherits work from a previous session that started v73at but never packaged it. The packaged v73at extends that work with the replyTo fix + offer_declined card. Earlier draft handoff for v73at (email-only scope) is superseded by this row |
| v73as | **Partner notification toggles (Part B of partner emails).** Completes what we started in v73al. Three-layer precedence model: (1) per-conversation per-event mute via `conv.notifyPartnerEvents = { event_key: false }`, (2) per-property universal at KV key `partner-notif:{slug}` with `{ universalMute, mutedEvents }`, (3) default = send. Master `conv.notifyPartner = false` still works as before (overrides everything for that thread). Six event types: new_enquiry, guest_reply, deposit_paid, offer_declined, change_request, stale_reminder. **Worker:** new helpers `loadPartnerNotifSettings(slug, env)` + `shouldSendPartnerEmail(eventType, conv, slug, env)`. Wired into the 6 partner email send sites (legacy booking POST, new-enquiry conv create, guest-reply/change-request branch, inbound-email reply, offer-declined, Stripe deposit-paid webhook, stale-conv cron). Each call passes the correct event type so per-event mute works. New endpoint `/api/partner-notif` (GET/POST), trust-by-slug like other pp-* endpoints. POST accepts either `{slug, universalMute, mutedEvents}` for property-wide settings, OR `{slug, convId, eventMute:{key:bool}}` for per-conv per-event override. **UI:** pp-notifications.html rebuilt from stub. Master "Pause all" toggle + 6 per-event switch rows with descriptions. Dirty-state tracking, Revert button, Save with success/error feedback. Reads ?as=X URL param. **Per-thread per-event UI:** gear icon added to pp-conversations.html thread header next to existing on/off slider. Click opens a 280px popover with 6 toggles; each saves immediately on change. The existing master notify on/off slider still works |
| v73ar | **Customer sidebar view transition fix \u2014 finally found it.** After v73aq's opacity:0-on-body change didn't help, Miguel's observation cracked it: "Conversations works \u2014 entire left hand menu and user profile stays static, and the right hand page loads." That described a working view transition. So Conversations IS transitioning; the others genuinely aren't. **Diffing the heads of conversations.html (working) vs bookings.html (broken) showed conversations is missing the flash-prevention CSS block entirely.** Whether the rule was `visibility:hidden` (v73g\u2013v73ap) or `opacity:0` (v73aq), applying it to `body` killed cross-document view transitions because the navigation target has no snapshot-able state for the browser to capture during the transition. **Fix:** moved the flash-prevention rule from `body` to `.account-wrap` (the inner content div that exists in all 6 customer pages). Body stays visible (allowing view-transition-name on the sidebar to work), inner content stays hidden until Clerk resolves (flash prevention preserved). Updated the 4 JS sites per page that flip the visibility to target `.account-wrap` instead of body. Updated noscript fallbacks similarly. The conversations.html page is unchanged \u2014 still has no flash-prevention rule, which is apparently fine for that page since the conversations layout doesn't show enough sensitive content to need it. **What I got wrong:** v73ap (relative URLs) and v73aq (opacity:0 on body) were both wrong guesses. The "lesson" from v73aq's handoff was right in spirit but I applied it to the wrong element. Should have moved the rule to a child wrapper, not just changed the CSS property |
| v73aq | **Customer sidebar view-transition fix — real root cause this time: `body { visibility: hidden }` in flash-prevention CSS.** After v73ap's "relative URLs" fix didn't help (I burned 5 builds on three failed theories before finding it), grep-diffing the working page (conversations.html) against a broken page revealed conversations was the ONLY customer page without `body { visibility: hidden }` in the flash-prevention CSS block. Pattern was added in v73g across all customer pages to prevent the 1-2s flash of account content after sign-out before the sign-in overlay mounts. **Cross-document view transitions are silently skipped when the navigation target has visibility:hidden on the body — the browser can't capture a meaningful "after" snapshot.** Conversations.html never received that v73g treatment for whatever reason, which is exactly why its sidebar nav was the only one that transitioned smoothly. **Fix:** replaced `body { visibility: hidden }` with `body { opacity: 0; transition: opacity .15s ease-in }` on the 6 affected customer pages (my-account, bookings, lens, saved, preferences, settings). Updated the 4 JS sites per page that flip the body back to visible to set `opacity = '1'` instead of `visibility = 'visible'`. Updated the noscript fallback similarly. **Why this works:** opacity:0 has identical flash-prevention semantics from the user's perspective (they see nothing until JS resolves auth) but the element remains in layout and snapshot-able for the view transition. Brief 150ms ease-in fade when the page resolves is actually a nicer effect than the abrupt visibility flip. **No other behavioral change.** Lesson learned (mine): when something works on Portal A and the identical-looking thing fails on Portal B, find the working version and diff it against the broken one BEFORE chasing exotic theories about routing, service workers, browser extensions, or absolute-vs-relative URLs. The non-trivial difference is almost always in the source, not the transport |
| v73ap | (Reverted theory — absolute vs relative URLs didn't fix it.) Stripped leading slash from customer sidebar hrefs to match admin/partner pattern. Kept the change since it matches admin/partner conventions, but it wasn't the bug |
| v73ao | Diagnostic instrumentation for customer unread-badge regression (logs in inbound handler + new /api/unread-debug endpoint for live counter inspection). No behavioral changes \u2014 read-only diagnostic |
| v73an | Four follow-up bugs from v73am, three from one root cause (case-sensitivity in PARTNER_EMAIL_TRANSITION_DEFAULT). Lowercased to 'nourelniltest213@gmail.com'; defensive lowercase in inbound classifier; build-offer primary CTA on new-enquiry partner email with deep-link to pp-bookings?newOffer={ref} auto-opening offer modal |
| v73al | Partner per-property email recipients (Part A). New `partner_emails` field on property record, edited via chip-list UI in admin-property-editor Section 0. 6 worker email paths now resolve recipients via `loadPartnerRecipients(slug, env)` with `NourElNilTest213@gmail.com` as transition fallback. Yellow "No partner email" pill on admin-properties cards |
| v73ak | Nights count computed from dates when not stored on booking record |
| v73aj | Three v73ai follow-ups: book-bar fallback for properties without per-room price, partner sidebar badges fixed when `?as=X` URL param is in use, custom dropdown no longer duplicates field on non-matching prefill |
| v73ai | Booking record now syncs trip details from offer on send + confirmation. Fixes "Dates TBD" on confirmed bookings when customer enquired open-endedly |
| v73ah | Five fixes from earlier smoke test: pill spacing, custom room dropdown, offer-modal alignment, seenByPartner/seenByAdmin badges + Bookings sidebar wire-up, confirmed-booking detail modals on both partner and admin portals |
| v73ag | Decline-offer flow chains the new enquiry data when present. If user fills in a fresh enquiry form and clicks Decline (instead of Request changes), the form data is no longer discarded \u2014 it's submitted as a follow-up message + updateEnquiry payload after the decline lands |
| v73af | Inline payment now confirms booking without depending on the webhook + offer dates backfill conv.enquiry. New `/api/checkout/sync-payment` endpoint (customer-side) and `/api/admin/sync-stripe-payment` rescue endpoint (admin-side). `postOfferCardToConversation` backfills empty `conv.enquiry` fields from the offer |
| v73ae | Partner offer-builder calendar UX matches customer enquiry form. Picking Arrival auto-opens Departure picker on same month, sets `min` constraint. Guarded against duplicate listeners when pricing mode toggles |
| v73ad | Styled offer + booking-confirmed cards on admin and partner conversations \u2014 parity with customer side. New shared `assets/offer-detail-modal.js` for read-only offer view, included on both `admin-conversations.html` and `pp-conversations.html` |
| v73ac | Customer Bookings badge \u2014 only count true customer action items. Removed the "fresh enquiry awaiting partner" branch added in v73aa. Now badge ONLY counts bookings in `offer_sent` state with an active offer awaiting customer response |
| v73ab | Two things: fixed regression where desktop card Enquire button still opened pay flow (post-hydration JS was overriding inline onclick), and shipped Stripe inline (Payment Element). Customer no longer leaves thebearing.io for payment. New `/api/checkout/create-intent` worker endpoint, webhook handles both checkout.session.completed and payment_intent.succeeded via normalization shim, new `assets/stripe-inline.js` client helper |
| v73aa | Five must-do polish items: fixed broken book bar Enquire button + hid Book now, conversation list shows enquiry dates for disambiguation, customer sidebar badge fixed (was counting all bookings due to wrong API param) + scoped to action items, expandable confirmed booking details with full offer snapshot, balance_due_date field added to offer schema |
| v73z | Critical hotfix for silent date mutation on already-confirmed bookings. Layer A: frontend filters reuse-candidate conversations by linked booking status (only enquiry/offer_sent/pending reuse). Layer B: worker guard expanded to three buckets (fresh enquiry mutates, offer-stage stores pendingChangeRequest, confirmed/cancelled does NOTHING with log warning) |
| v73y | Six fixes from v73x Stripe smoke test feedback: receipt wording updated, partner sidebar bookings badge counts only items needing action, room dropdown in offer builder via datalist, customer-side flag when offer room differs from enquiry, partner conversations badge fixed (recompute call after webhook write), timing instrumentation on offer modal load |
| v73x | Stripe smoke test fixes from v73w first run: persistent "Booking confirmed" hero card replacing the 8s toast that was too easy to miss, webhook fallback for missing conversationId so system message gets posted, explicit diagnostic logging at every webhook branch |
| v73w | Email infrastructure: split reply receiving onto `replies.thebearing.io` subdomain so root MX can flip to Google. admin@thebearing.io now receives external mail normally |
| v73w | Email infrastructure: split reply receiving onto `replies.thebearing.io` subdomain so root MX can flip to Google. DNS records added in Cloudflare (DKIM/SPF/MX), Resend domain verified, Receiving enabled. Worker changes: reply_to switched to subdomain (envoy.js:1930), inbound regex broadened to match both root and subdomain (envoy.js:2263). Phase 4 tested working end-to-end. Root MX subsequently flipped to Google ASPMX records, Resend inbound MX deleted. admin@thebearing.io now receives external mail normally |
| v73v | Email delivery diagnostics: richer Resend health check, new "Send test email" maintenance button (`POST /api/test-email`). Used to root-cause why admin@thebearing.io receives no mail — diagnosis: root MX records pointed at Resend's inbound, not Google, so mail to @thebearing.io went into Resend's void instead of reaching Gmail |
| v73v | Email delivery diagnostics: richer Resend health check (surfaces whether thebearing.io is verified as a sending domain), new "Send test email" maintenance button (`POST /api/test-email`). Used to root-cause why admin@thebearing.io receives no mail — diagnosis: root MX records pointed at Resend, not Google, so mail to @thebearing.io went into Resend's inbound void instead of reaching Gmail |
| v73u | Five fixes + change-request email upgrade. Decline now copies pendingChangeRequest values back to booking before clearing it. Sand-colored "Previously declined offer" row treatment on pp-bookings. Removed bullet from change-request pill. Revise modal banner relayout below header. Property thumbnails on customer /bookings rows via parallel prefetch + cache. BONUS: distinct "Change request from..." email subject/body when a guest message triggers pendingChangeRequest storage |
| v73t | Three v73r/s bug fixes from Miguel's testing. fmtDateRange() returning HTML markup got escaped and rendered as visible text in the change-request pill + Revise modal banner \u2014 added fmtDateRangePlain() helper. Decline-from-modal Decline button added to /conversations and /bookings offer modals (removed stale v73q placeholder copy while editing). Perceived-latency fix on Request changes button: now shows "Sending your request\u2026" immediately on click |
| v73s | admin-settings layout hotfix: v73r removed an extra `</div>`, leaving the new "Restore booking dates" block floating outside the parent card. Restored |
| v73r | Fixed silent data-corruption bug where re-enquiry overwrote booking dates while offer stayed frozen. Worker now stores pendingChangeRequest instead of mutating dates when an active offer exists. New /api/offer action:'decline' endpoint. Partner pp-bookings rows surface change requests with amber row + pill + Revise modal banner. Customer interstitial on /property when active offer exists (View / Request changes / Decline). Admin maintenance button to restore booking dates from active offer |
| v73q | **Stripe Checkout integration end-to-end.** Customer can now accept an offer, pay deposit via Stripe-hosted Checkout, booking auto-confirmed via webhook with notifications. New endpoint POST /api/checkout/create-session (public, email-auth). Webhook handler for checkout.session.completed: marks booking confirmed+paid, marks offer accepted, posts booking_confirmed system message, sends 3 Resend emails. Idempotent via paymentStatus+sessionId guard. Frontend: Accept & pay buttons wired in both conversation + bookings offer modals. /bookings.html parses ?checkout=success|cancelled, shows toast banner. booking_confirmed message renders as moss-green card. **Partner email recipient is currently admin@thebearing.io as placeholder** (no per-property partner contact yet). **STRIPE_WEBHOOK_SECRET needs setting in CF Pages env vars + redeploy** \u2014 documented in shipping notes |
| v73p | Chevron hotfix that should have shipped with v73o. \\u203a inside HTML markup rendered as literal "u203a" instead of "\u203a" because JavaScript escape syntax doesn't work in raw HTML. Replaced with literal "\u203a" character |
| v73q | **Stripe Checkout integration end-to-end.** Customer can now accept an offer, pay the deposit via Stripe-hosted Checkout, and the booking is auto-confirmed via webhook with notifications to all parties. (1) **`POST /api/checkout/create-session`** (public, email-auth) validates the offer (status='sent', not expired, deposit ≥ $0.50), verifies requester's email matches `booking.email`, checks booking isn't already confirmed, then creates a Stripe Checkout session with metadata `{offerId, bookingRef, propertySlug, conversationId}`. Session expires in 30min. Success URL: `/bookings.html?checkout=success&session_id=X`. Cancel URL: `/bookings.html?checkout=cancelled`. Statement descriptor suffix: `BEARING`. Returns `{url}` for frontend redirect. (2) **`POST /api/stripe/webhook`** signature-verified via `constructEventAsync` (already scaffolded). Handler for `checkout.session.completed` now: marks booking `status:'confirmed', paymentStatus:'deposit_paid', stripeSessionId, stripePaymentIntent, depositPaidAmount, depositPaidAt`; marks offer `status:'accepted', responded_at, stripeSessionId`; posts a `type:'booking_confirmed'` system message into the linked conversation ("✓ Deposit paid · Booking confirmed · TB-XXXX-XXXX"); sends three Resend emails (customer confirmation, admin notification, partner notification). **Idempotent against Stripe retries** via guard: if booking is already `deposit_paid` with the same session ID, returns 200 OK without re-mutating or re-sending emails. Catches all errors and still 200s the webhook so Stripe doesn't retry our infra bugs. (3) **Accept & pay buttons wired in both offer modals.** `/conversations.html` and `/bookings.html` versions (copy-pasted modal still — sharing pending) both now POST to `/api/checkout/create-session` with the user's Clerk email and redirect to the returned Stripe URL on success. Error states surface as alert with the worker's specific error message. (4) **`/bookings.html` handles checkout return state**: detects `?checkout=success` or `?checkout=cancelled` in URL on load, shows a top-anchored toast banner (moss-green success, sand cancelled), then strips the query params via `history.replaceState` so refresh doesn't re-show. Success auto-dismisses after 8s; cancelled stays until tapped. (5) **`booking_confirmed` message rendering in customer conversation**: moss-green card with checkmark icon, title, body, and "View booking →" CTA. Partner + admin views see the plain-text fallback as a regular bubble (sufficient — they aren't the audience for celebratory styling). Render branch added before the `else` text-bubble fallback in `chRenderMessagesInto`; hover actions skipped on these system cards. **Architecture notes:** Email-based auth (requesterEmail must match booking.email) is the same pattern as cancel-enquiry (v73m). Pre-existing concern: knowledge of booking ref + email would let a third party initiate checkout for someone else's booking — financial harm accrues to attacker, not victim, so risk is low. Real fix is Clerk session verification, still deferred. **Partner email recipient is currently `admin@thebearing.io` as a placeholder** — properties don't have a partner contact email field yet (no real partner auth). Flagged for the future build where multi-tenant partner accounts land. **Out-of-band step for Miguel:** STRIPE_WEBHOOK_SECRET still isn't set in CF Pages env vars. Steps: deploy v73q → Stripe Dashboard → Developers → Webhooks → Add endpoint `https://thebearing.io/api/stripe/webhook` → select event `checkout.session.completed` → copy signing secret → add to CF Pages env vars as `STRIPE_WEBHOOK_SECRET` → trigger a redeploy (any commit). Until that's done, checkout creation works but payment confirmations won't process |
| v73p | Chevron hotfix that should have shipped with v73o. `<span class="bk-cancelled-chev">\u203a</span>` rendered as literal "u203a" because JavaScript escape syntax doesn't work in raw HTML. Now uses literal `›` character |
| v73o | **Archived conversation surface: read-only mode + cancelled section on /bookings + sidebar archive toggle.** Miguel asked whether cancelling an enquiry deleted the conversation. Investigated: no, the conversation is marked `status:'archived'` in KV with full message history preserved, but it was filtered out of every UI with no path back. Three changes plus a worker guard to surface that archived state safely. (1) **Worker hardens against direct-API message bypass.** `/api/conversation` POST with `action:'message'` now checks `conv.status === 'archived'` and returns `409 conversation is archived (read-only)` before any state mutation. Belt-and-suspenders even though the frontend disables the composer. (2) **Read-only mode across all three conversation portals** (customer, partner, admin — full parity). When `conv.status === 'archived'`, the page renders: (a) a sand-colored banner between header and messages with copy varying by `archivedReason` ("You cancelled this enquiry" / "The guest cancelled this enquiry" / "This enquiry was cancelled by admin" / generic fallback), archived date shown if available, archive-box icon; (b) composer fully replaced with a "This conversation is closed" notice strip — no disabled-but-visible textarea, just a clean read-only state; (c) muted styling cascades to messages via `.thread-archived` / `.ch-thread-archived` class (light tan messages background, bubbles at 78% opacity). All three portal CSS files got the same `.archived-banner` / `.archived-composer-notice` rules; customer version uses `ch-` prefix to match its existing namespace. (3) **Fourth "Cancelled" section on customer /bookings**, using a native `<details>` element collapsed by default. Renders only when there are cancelled bookings (`display:none` otherwise — no empty state). Muted gray panel with chevron-summary header showing count + "Read-only archive" hint, no accent stripe (unlike active sections). Rows are anchor links to the linked archived conversation. Bucketing now captures cancelled into its own array instead of dropping. Section subtitle and pill counts continue to reflect only active bookings (cancelled don't inflate the "X total" header). (4) **"Show archived" toggle in customer conversation sidebar.** New globals `chConvsAll` (full set) and `chShowArchived` (filter state, session-only — not persisted). Fetch and 15s refresh populate `chConvsAll`; `chApplyArchiveFilter()` derives the visible `chConvs`. Toggle button below the title row, hidden when no archived conversations exist. **Auto-flips ON when the URL `?id=X` points to an archived conversation** — important for users landing here from the /bookings cancelled section, otherwise they'd see an active conversation in the thread pane with no matching sidebar item. Archived items in the sidebar get an `archived` class: 62% opacity, plus a sand-colored "archived" pill appended to the property name via CSS pseudo-content. Toggle button label flips between "Show archived (N)" and "Hide archived". **Brace audit:** pre-existing `@keyframes ciDot` malformation in bookings.html (delta -1) is still present — confirmed harmless via CSS error recovery, v73o added 15 new rule pairs but didn't fix or worsen it. Orphan-opener scanner run before shipping; no v73m-class bugs introduced |
| v73n | /bookings page CSS hotfix — v73m's str_replace left an orphan `.bk-section-pill {` opener that broke all v73m styles. v73n removes the orphan; otherwise identical to v73m. Pre-existing `@keyframes ciDot` malformation in bookings.html documented as benign |
| v73m | (defective — superseded by v73n) Section tints, colored pills, cancel-enquiry mechanism, admin batch cancel for stale enquiries, offer-card backfill, flash protection on lens/saved/preferences/settings |
| v73l | Interim: arrow-bug fix, redundant Room cell removed, sections reordered (Offers first), CSS hooks for v73m color treatment |
| v73k | Customer /bookings.html rewritten with real KV data + three sections. Offer detail modal ported from conversations.html. body-flash-protection on bookings |
| v73j | Enquiry message reformat. "Average nightly" labels. Save section defaults per property. Offer surfaces as card in conversation thread with full-detail modal |
| v73i | Second-enquiry silent-discard fix. Backfill endpoint for stub bookings. Auth modal Grid centering |
| v73h | "Your Cove" → "Your Compass" |
| v73g | Stub-booking on enquiry create + four smoke-test fixes |
| v73f | `?as=X` partner switching |
| v73e | Bulletproof sign-out, "On request" fallback |
| v73d | openEnquiry auth-gating |
| v73c | Killed Step 3 enquiry |
| v73b | property.html universal-template hardening |
| v73a | Adults/Children inputs |
| v72z | Critical: unclosed populateConfirmation() broke property.html |
| v73k | Customer /bookings.html rewritten with real KV data and three sections. Worker `/api/booking?email=` for customer-scoped lookup. Body flash-protection. Offer detail modal ported from conversations.html |
| v73j | Enquiry message reformat. "Average nightly" labels. Save section defaults per property. Offer surfaces as card in conversation thread with full-detail modal |
| v73i | Second-enquiry silent-discard fix. Backfill endpoint for stub bookings. Auth modal Grid centering |
| v73h | "Your Cove" → "Your Compass" |
| v73g | Stub-booking on enquiry create + four smoke-test fixes |
| v73f | `?as=X` partner switching |
| v73e | Bulletproof sign-out, "On request" fallback |
| v73d | openEnquiry auth-gating |
| v73c | Killed Step 3 enquiry |
| v73b | property.html universal-template hardening |
| v73a | Adults/Children inputs |
| v72z | Critical: unclosed populateConfirmation() broke property.html |
| v73j | Enquiry message reformatted (paragraph blocks instead of run-on sentence). "Average nightly" labels throughout offer builder. Partner can save section defaults per property (Inclusions, Cancellation terms, Notes). Offer surfaces as styled card in customer conversation thread + opens modal with full detail + Accept/Request actions |
| v73i | Second-enquiry silent-discard bug fixed. Backfill endpoint for stub bookings. Auth modal Grid centering |
| v73h | "Your Cove" → "Your Compass". Back-to-Bearing links on signed-out screens |
| v73g | Five smoke-test fixes including stub-booking on enquiry create |
| v73f | `?as=X` partner switching, OAuth resume sessionStorage, auth modal width bump |
| v73e | Bulletproof sign-out, "On request" fallback, price_indicator field |
| v73d | openEnquiry auth-gating, async Clerk resolution, hide empty From |
| v73c | Killed Step 3 enquiry. Listing-page enquiry redirects |
| v73b | property.html universal-template hardening |
| v73a | Adults/Children inputs |
| v72z | **Critical: unclosed `populateConfirmation()` broke property.html** |
| v73i | Second-enquiry silent-discard bug fixed (now posts as follow-up message). Backfill endpoint for stub bookings. Auth modal centering: CSS Grid with `place-items:start center` + explicit viewport dimensions |
| v73h | "Your Cove" → "Your Compass" rename. Clickable Bearing wordmark + Back-to-Bearing link on all signed-out screens |
| v73g | Five smoke-test fixes: my-account flash protection; auth modal align-items:flex-start; index.html bounces tb_pending_action; enquiry creates linked stub booking; PP identity dynamic |
| v73f | Auth modal max-width 480→560 + Loading placeholder removed + clean-URL paths. `tb_pending_action` sessionStorage for OAuth resume. `?as=X` partner switching |
| v73e | Bulletproof sign-out. Graceful "On request" fallback. price_indicator field |
| v73d | openEnquiry wraps tbAuth.requireUser. Robust Clerk-user resolution. Empty "From" hidden when no price |
| v73c | Killed Step 3 enquiry. Robust Clerk resolution. Listing-page enquiry redirects to property page. Inline error display |
| v73b | property.html universal-template hardening: removed kvHydrate IIFE, stashed window.PROPERTY_SLUG |
| v73a | Side-by-side Adults/Children inputs. Fixed undefined submitEnquiryAndGo |
| v72z | **Critical: unclosed `populateConfirmation()` function broke property.html** |
| v73h | "Your Cove" → "Your Compass" rename across my-account.html and conversations.html. Clickable Bearing wordmark + "← Back to The Bearing" link added to all six signed-out overlay screens so users aren't trapped |
| v73g | Five smoke-test fixes: my-account flash protection; auth modal `align-items:flex-start` for tall viewports; index.html bounces `tb_pending_action` to property; enquiry creates linked stub booking with `status:'enquiry'`; PP identity dynamic via `fetchPartnerIdentity()` |
| v73f | Auth modal max-width 480→560 + remove Loading placeholder + clean-URL paths in stayInPlace. `tb_pending_action` sessionStorage for post-OAuth resume. `?as=X` URL param for partner-as switching |
| v73e | Bulletproof sign-out (waits for Clerk, clears sessionStorage, hard-redirect with cache-bust). Graceful "On request" fallback. New `price_indicator` field |
| v73d | Auth race fix: `openEnquiry()` wraps `tbAuth.requireUser()`. Robust async Clerk-user resolution. Empty "From" hidden when no price |
| v73c | Killed Step 3 of enquiry flow. Robust Clerk user resolution. Listing-page enquiry buttons redirect to property page. Inline error display. Pre-existing villas.html duplicate `.catch()` cleaned up |
| v73b | Property.html universal-template hardening: removed obsolete `kvHydrate()` IIFE, stashed URL slug as `window.PROPERTY_SLUG`, replaced fake conversation preview on Step 3 |
| v73a | Side-by-side Adults/Children number inputs. Fixed undefined `submitEnquiryAndGo()`. Cleaned orphaned SVG debris |
| v72z | **Critical fix: property.html had an unclosed `populateConfirmation()` function** that broke the entire page. Bug pre-dated v72y |
| v73g | Five smoke-test fixes: my-account flash protection via `body{visibility:hidden}`; auth modal `align-items:flex-start` for tall viewports; index.html bounces `tb_pending_action` to property; enquiry creates linked stub booking with `status:'enquiry'`; PP identity dynamic via `fetchPartnerIdentity()` |
| v73f | Auth modal max-width 480→560 + remove Loading placeholder + clean-URL paths in stayInPlace. `tb_pending_action` sessionStorage for post-OAuth resume. `?as=X` URL param for partner-as switching across pp-bookings, pp-conversations, sidebar-badges |
| v73e | Bulletproof sign-out (waits for Clerk, clears sessionStorage, hard-redirect with cache-bust). Graceful "On request" fallback. New `price_indicator` field |
| v73d | Auth race fix: `openEnquiry()` wraps `tbAuth.requireUser()`. Robust async Clerk-user resolution. Empty "From" hidden when no price |
| v73c | Killed Step 3 of enquiry flow. Robust Clerk user resolution. Listing-page enquiry buttons redirect to property page. Inline error display. Pre-existing villas.html duplicate `.catch()` cleaned up |
| v73b | Property.html universal-template hardening: removed obsolete `kvHydrate()` IIFE, stashed URL slug as `window.PROPERTY_SLUG`, replaced fake conversation preview on Step 3 |
| v73a | Side-by-side Adults/Children number inputs. Fixed undefined `submitEnquiryAndGo()`. Cleaned orphaned SVG debris |
| v72z | **Critical fix: property.html had an unclosed `populateConfirmation()` function** that broke the entire page. Bug pre-dated v72y |
| v73f | Auth modal max-width 480→560 + remove Loading placeholder + clean-URL paths in stayInPlace. `tb_pending_action` sessionStorage for post-OAuth resume. `?as=X` URL param for partner-as switching across pp-bookings, pp-conversations, sidebar-badges |
| v73e | Bulletproof sign-out (waits for Clerk, clears sessionStorage, hard-redirect with cache-bust). Graceful "On request" fallback. New `price_indicator` field |
| v73d | Auth race fix: `openEnquiry()` wraps `tbAuth.requireUser()`. Robust async Clerk-user resolution. Empty "From" hidden when no price |
| v73c | Killed Step 3 of enquiry flow. Robust Clerk user resolution. Listing-page enquiry buttons redirect to property page. Inline error display. Pre-existing villas.html duplicate `.catch()` cleaned up |
| v73b | Property.html universal-template hardening: removed obsolete `kvHydrate()` IIFE, stashed URL slug as `window.PROPERTY_SLUG`, replaced fake conversation preview on Step 3 |
| v73a | Side-by-side Adults/Children number inputs. Fixed undefined `submitEnquiryAndGo()`. Cleaned orphaned SVG debris |
| v72z | **Critical fix: property.html had an unclosed `populateConfirmation()` function** that broke the entire page. Bug pre-dated v72y |
| v73e | Bulletproof sign-out (waits for Clerk to load, clears sessionStorage, hard-redirect with cache-bust). Graceful "On request" fallback for properties with no price. New `price_indicator` field on properties (free-text seasonal guidance, shown on booking sidebar) |
| v73d | Auth race fix: `openEnquiry()` wraps `tbAuth.requireUser()` like other Enquire buttons. Robust async Clerk-user resolution. Empty "From" hidden when no price |
| v73c | Killed Step 3 of enquiry flow. Robust Clerk user resolution. Listing-page enquiry buttons redirect to property page. Inline error display. Pre-existing villas.html duplicate `.catch()` cleaned up |
| v73b | Property.html universal-template hardening: removed obsolete `kvHydrate()` IIFE that hardcoded SLUG='nour-el-nil', stashed URL slug as `window.PROPERTY_SLUG`, replaced fake conversation preview on Step 3 |
| v73a | Side-by-side Adults/Children number inputs. Fixed undefined `submitEnquiryAndGo()`. Cleaned orphaned SVG debris |
| v72z | **Critical fix: property.html had an unclosed `populateConfirmation()` function** that broke the entire page. Bug pre-dated v72y |
| v73d | Auth race fix — `openEnquiry()` now wraps `tbAuth.requireUser()` like the other Enquire buttons (was a non-auth-gated shortcut). `submitEnquiryAndOpenChannel` async-loads Clerk if needed, opens sign-in modal on failure instead of dead-end error. Empty "From" price block hidden when property has no rooms |
| v73c | Killed Step 3 of enquiry flow. Robust Clerk user resolution. Listing-page enquiry buttons redirect to property page. Inline error display. Removed pre-existing duplicate `.catch()` in villas.html |
| v73b | Property.html universal-template hardening: removed obsolete `kvHydrate()` IIFE that hardcoded SLUG='nour-el-nil', stashed URL slug as `window.PROPERTY_SLUG`, replaced fake conversation preview on Step 3 |
| v73a | Replaced single Guests text field with side-by-side Adults/Children number inputs. Fixed undefined `submitEnquiryAndGo()` button. Cleaned orphaned SVG/`</a>` debris |
| v72z | **Critical fix: property.html had an unclosed `populateConfirmation()` function** that broke the entire page. Bug pre-dated v72y |
| v73c | Killed Step 3 of enquiry flow (Step 2 button now creates conversation and navigates directly). Robust Clerk user resolution. Listing-page enquiry buttons redirect to property page (no more fake "Enquiry sent" toast that does nothing). Inline error display. Removed pre-existing duplicate `.catch()` in villas.html |
| v73b | **Property.html universal-template hardening:** removed obsolete `kvHydrate()` IIFE that hardcoded SLUG='nour-el-nil', stashed URL slug as `window.PROPERTY_SLUG`, fixed populateConfirmation to use it, replaced fake iPhone conversation preview on Step 3 with clean confirmation panel |
| v73a | Replaced single text Guests field with side-by-side Adults/Children number inputs. Fixed "Open my conversation" button calling undefined `submitEnquiryAndGo()`. Cleaned orphaned SVG/`</a>` debris on Step 3 |
| v72z | **Critical fix: property.html had an unclosed `populateConfirmation()` function** that broke the entire page. Bug pre-dated v72y |
| v73b | **Property.html universal-template hardening:** removed obsolete `kvHydrate()` IIFE that hardcoded SLUG='nour-el-nil', stashed URL slug as `window.PROPERTY_SLUG`, fixed populateConfirmation to use it, replaced fake iPhone conversation preview on Step 3 with clean confirmation panel |
| v73a | Replaced single text Guests field with side-by-side Adults/Children number inputs. Fixed "Open my conversation" button calling undefined `submitEnquiryAndGo()`. Cleaned orphaned SVG/`</a>` debris on Step 3 |
| v72z | **Critical fix: property.html had an unclosed `populateConfirmation()` function** that broke the entire page. Bug pre-dated v72y |
| v72y | **Offer model + partner offer builder + pp-bookings rewritten with real data.** Worker: new `/api/offer` endpoint family (GET by id or bookingId, POST creates draft/sends immediately, PATCH for send/withdraw/edit-draft). Snapshots `commission_pct` into `commission_pct_at_time` at offer creation so future property edits don't alter sent offers. New KV: `offer:{id}`, `__offers_by_booking:{ref}`. Booking records gain `offers: []` + `active_offer_id` (on-read backfill). `/api/booking?slug=X` filter added. pp-bookings: dummy data deleted, real KV data, state-aware action buttons. Offer builder modal: trip dates auto-compute nights, pricing mode toggle, live deposit preview, repeatable inclusion items, exclusions, cancellation terms (required), notes, validity date. Save as draft OR send. Revisions auto-fill via `supersedes`. **Partner auth still hardcoded** (`PP_SLUG = 'nour-el-nil'`) — real multi-tenant partner auth deferred |
| v72x | **`commission_pct` + `booking_type` fields added to property schema.** Worker: `/api/property` GET backfills `commission_pct: null` and `booking_type: 'enquiry'` on legacy records (idempotent, in-memory). POST validates `commission_pct` is null or 1–25, `booking_type` must be `'enquiry'` (only valid v1 value). admin-property-editor: new "Section 0: Commercial terms" at top of Listing tab, marked Admin-only, with commission % input (inline validator with $5K example) and booking type select (instant-book reserved/disabled). admin-properties: each card shows commission pill — gray "15% commission" when set, amber "Commission not set" when null |
| v72w | **Stripe SDK scaffolding — connection ready, no flow yet.** New `package.json` with `stripe: ^17.0.0`. Stripe import in worker using `FetchHttpClient` (Workers don't have `node:https`). New `GET /api/stripe/health` (admin) + `POST /api/stripe/webhook` (signature-verified via `constructEventAsync` — sync `constructEvent` fails silently on Workers). `/api/health` extended with Stripe check. admin-settings System Health card shows Stripe row. admin-payments banner probes Stripe health on load and flips green/red/amber based on state. Required env vars: `STRIPE_SECRET_KEY` + `STRIPE_PUBLISHABLE_KEY` in Cloudflare Pages (encrypted). `STRIPE_WEBHOOK_SECRET` not needed until v72z |
| v72v | **admin-payments dummy data killed + payments architecture locked.** Replaced hardcoded $2.41M GMV / $361k commission / fake transactions / fake disputes with honest empty state. Status banner, em-dash KPI tiles, empty transactions card, roadmap panel. **Booking model decisions locked (apply to all subsequent payment builds):** (1) **Deposit-only model** — TheBearing collects only its negotiated commission percentage as non-refundable booking fee. Partner arranges balance directly with member. (2) **All USD** — partners enter USD; member pays USD; no FX risk in TheBearing data model. (3) **Per-property `commission_pct`** — variable per property (UI in v72x). (4) **Enquiry-only booking flow for v1** — no instant-book / channel-manager integrations until partner-specific need. (5) **Pricing supports both per-night × nights AND package total** — partner chooses per offer. Single nightly rate, no mid-stay changes for v1. (6) **Offer revisions, not counter-offers** — partner builds a new offer that supersedes the previous one |
| v72u | **Removed misplaced "Property partner? Sign in" link from customer footer on 6 customer pages** (lens, saved, bookings, preferences, settings, search). Dark footer bar stays — only the partner link was removed |
| v72t | **View transitions extended to partner + customer portals.** Renamed `admin-chrome.css` → `portal-chrome.css` with portal-agnostic transition name. Linked from 33 portal pages (11 admin + 13 partner + 9 customer). 245 internal portal hrefs cleaned of `.html`. Badge scripts updated to match both URL forms |
| v72s | **Found the actual root cause of the navigation flash: redirect-skip.** Cloudflare Pages canonicalizes URLs by redirecting `/admin-foo.html` → `/admin-foo` (clean URL). Cross-document view transitions are explicitly skipped by browsers when a navigation involves a redirect. Fix: stripped `.html` from all 22 internal admin links so navigations go straight to the canonical URL with no redirect hop. Also bumped `SIDEBAR_VERSION` to 2 in `admin-sidebar-inject.js` so existing browsers don't serve the old cached sidebar with the `.html` links |
| v72p | **Conversation scroll-to-bottom fix across admin / customer / partner portals.** New shared helper `assets/conv-scroll.js` exposes `window.pinToBottom(wrap, opts)` — uses `scrollIntoView({block:'end'})` on the last child, repeats across 3 animation frames to catch late reflows, attaches a 1-second ResizeObserver so image-load / reaction-render reflows re-pin. Applied at all 6 sites: open-conv + polling-refresh in admin-conversations.html / conversations.html / pp-conversations.html |
| v72o | **admin-properties.html: dead prospecting code removed + listing-order regression fix.** The AI prospecting kanban (kanban + AI email composer + Resend send + template gallery + reply tracker) was already non-functional. Removed ~470 lines of JS, 4 dead modals, unused CSS. File shrunk 1281 → 786 lines. Also fixed v72m regression: worker now special-cases `__index_hotels`/`__index_cruises`/`__index_villas` writes so the Listing Order save button works again |
| v72n | **Admin sidebar consolidated to a cached partial — no more flash on nav.** Extracted sidebar HTML to `assets/admin-sidebar.html`. New `assets/admin-sidebar-inject.js` reads `sessionStorage['tb_admin_sidebar_v1']` and injects synchronously on cache hits. First-ever load this session fetches the partial once (~40ms), caches it, and re-runs the badge + user-name hooks. Every admin page now has `<body data-page="X">` so inject script can mark the right link active. Net: −40kB across the 11 admin pages, no flash on subsequent nav. Bump `SIDEBAR_VERSION` in inject.js when changing the partial |
| v72m | **Property index hygiene + slug validation.** Three garbage slugs were showing up in admin-analytics' Live Listing Health: `__index_villas`, `__index_hotels`, `__index_cruises` (curated-order metadata keys that got POSTed as property records in an early version of the reorder feature) plus `singita-grumeti-x` (empty stub record). **Fixes:** worker GET defensively filters `__*` slugs; POST validates slug format strictly; new `POST /api/property/cleanup` endpoint; admin-settings.html has a "Maintenance" section with a "Clean property index" button; admin-analytics double-filters client-side |
| v72l | **admin-analytics.html rebuilt from scratch — zero dummy data.** All hardcoded values gone (`$2.41M`, `$612k`, fake top-properties list, traffic-sources guesses, conversion-funnel made-up rates, fake member-growth chart). New layout reads exclusively from `/api/members`, `/api/booking`, `/api/conversation`, `/api/property`. Sections: (1) KPIs — Members / Enquiries / Confirmed bookings / GMV, all period-filtered; (2) Activity over time — 3-series SVG line chart with member/enquiry/booking buckets by day/week/month based on period; (3) Top properties by enquiry volume — sorted, with bookings + GMV per row; (4) Booking pipeline — confirmed/pending/cancelled split with value sums; (5) Member sign-in providers — Google/Apple/Email breakdown with bars; (6) Enquiry response performance — total / replied / awaiting / avg-wait / longest-waiting with color thresholds; (7) Live listing health — preserved KV completeness scorer. Date tabs (7d/30d/90d/12m/All) now actually filter. CSV export works — produces multi-section CSV (Members, Enquiries, Bookings) with KV data, filtered to current period. Honest empty states everywhere — "No enquiries in last 30 days yet." rather than synthesised numbers |
| v72k | Stale-conversation cron diagnostics + manual trigger. **Symptom:** Cloudflare Cron events showed 12+ successful hourly runs but the System Health card stayed "no runs recorded." **Root cause:** the cron's early-return guard `if (!env.DOSSIERS || !env.RESEND_API_KEY) return` exited before reaching the `__cron:last_run` KV write — so successful-but-skipped runs were invisible. Suggests `RESEND_API_KEY` may not be injected into the scheduled() handler even when bound to the request handler (known Pages Functions quirk). **Fix:** (a) cron now writes `__cron:last_run` on every run including skips, with a `skipped` reason field; (b) new endpoint `POST /api/cron/run` (admin-gated) triggers the cron synchronously and returns the run summary; (c) admin-settings.html System Health card has a "Run now" button on the cron row that fires the manual trigger and refreshes the row. Health card cron detail now distinguishes "skipped" / "errored" / "last ran X" |
| v72j | Admin auth fix for multi-email Clerk accounts. **Root cause:** Miguel's Clerk account has `mcancino@gmail.com` as primary email and `admin@thebearing.io` as a secondary. `admin-gate.js` was iterating all linked emails so the client gate accepted him, but `admin-fetch.js` was only sending the primary email in `X-Admin-Email` — so the worker saw `mcancino@gmail.com`, found it not in the allowlist, and 403'd. **Fix:** `admin-fetch.js` now sends ALL of the user's emails as a comma-separated `X-Admin-Email` header. Worker Path 2 splits on commas and accepts if any candidate is in the allowlist. Confirmed via worker log line `[Admin] Email header path: not in allowlist. email= mcancino@gmail.com allowlist= admin@thebearing.io` after v72i's diagnostic logging |
| v72i | Admin auth hardening: `isAdmin()` no longer returns `false` outright when Path 1 (Clerk session verify) attempts and fails — it now always falls through to Path 2 (X-Admin-Email header check) as a fallback. Previously, an attempted-but-failed Clerk verify (e.g. session token format mismatch, transient Clerk API error) returned false immediately, even when a valid X-Admin-Email header was present, causing 403s on `/api/settings` and `/api/health`. Added diagnostic console.log lines on every failure branch so Cloudflare Pages worker logs show exactly which path failed and why |
| v72h | Email policy update: `admin@thebearing.io` is the only address in use. Notification baseline `BASELINE_NOTIFICATION_RECIPIENT` in worker switched from `miguel@thebearing.io` → `admin@thebearing.io`. admin-settings.html help text updated. admin-properties.html: prospecting-email signature address and "From email" placeholder both updated to `admin@thebearing.io`. v72g build-history row left as-is (historical record). HANDOFF booking-system note updated to reflect new baseline |
| v72g | **admin-settings.html built out.** Three sections: (1) Notification recipients — chip-list editor, baseline `miguel@thebearing.io` locked & always included; (2) Admin allowlist — chip-list editor, baseline `admin@thebearing.io` locked; (3) System health — live checks for KV, Resend (validates API key via `/domains`), Vectorize (`describe()`), Workers AI, Anthropic, Clerk, and cron last-run. Worker: `__settings:notifications` and `__settings:allowlist` KV-backed; 5 hardcoded `to:['miguel@thebearing.io']` recipients refactored to `loadNotificationRecipients()`; `ADMIN_EMAILS` refactored to `getAllowlist()` merging baseline + KV extras. New endpoints: `/api/settings` (GET/POST admin-gated), `/api/settings/allowlist-public` (GET unauth — used by client gate), `/api/health` (GET admin-gated). `assets/admin-gate.js` extended to fetch dynamic allowlist on every admin page load, 3s cap, baseline-only on failure. Cron persists `__cron:last_run` with `{ranAt, durationMs, scanned, sent, ok}` for the health check |
| v72f | (build packaged but history row not recorded — inherited zip from previous session)  |
| v72e | Envoy drawer positioning fix: lens/saved/bookings/preferences had dark-theme styling but were missing `position:fixed; right:0; width:25vw; transform:translateX(100%)` base rules — drawer rendered without positioning, breaking layout. Patched all 4 pages with full positioning + open-state + body-push + responsive rules |
| v72d | Conversation pages dead-space fix at bottom of viewport: customer (`account-wrap` padding-top + height calc overflowed by 66px → set `height:100dvh; box-sizing:border-box`), admin (was subtracting 60px for a topbar that doesn't exist → `height:100vh`), partner (`calc(100vh - 200px)` over-budgeted → `flex:1; min-height:0`) |
| v72c | property.html hardening: 12s safety timeout, granular error messages instead of generic, console diagnostics, isolated renderProperty try/catch, loadSimilar chained as `.catch()` so it can't block main render. Fixed `/property?slug=` (no .html) in similar cards + admin-property-editor preview button |
| v72b | Customer page "View property" action: special-cases `nour-el-nil` slug → `/nour-el-nil.html` (dedicated hand-built page), all other slugs → dynamic `/property.html?slug=...` template |
| v72 | Customer conversations full visual parity: 3-column layout (responsive, collapses <1100px), property details panel (hero image, facts, your enquiry, View property + Save to wishlist), Clerk photo for guest avatars, role-distinct bubbles, hover actions, reactions (guest scoped), sound (`tb_guest_sound`), animations, day separators, ⌘+Enter to send |
| v71 | Partner portal conversations parity: 3-column with partner-scoped guest context panel (no LTV/notes/saved-replies — admin only). Previous conversations filtered to `propertySlug === PP_SLUG` (privacy). Sound key `tb_pp_sound` |
| v70g | Emoji popover diagonal-mouse fix: popover centered above smiley btn, invisible bridge below popover, `.actions-pinned` class on parent action bar via JS + CSS `:has()` so the action bar stays visible while picker open |
| v70f | Session 3 admin polish: emoji reactions (6 emoji, per-message KV via `action:'reaction'`), saved replies (`/api/saved-replies` GET/POST/DELETE admin-gated, stored at `__saved_replies`), sound on incoming msgs (Web Audio chirp, `tb_admin_sound`), message fade-in animation, click-outside closes popovers |
| v70e | Session 2 admin polish: right-side guest context panel (avatar, presence, stats grid Joined/Convs/Bookings/LTV, Tier/Location/Provider, Other conversations, Internal notes with 800ms autosave). Hover actions on messages (emoji react / reply-quote / copy with toast confirmation). Quoted reply prepends `> Sender wrote:` in send |
| v70d | (folded into v70e) |
| v70c | (folded into v70e) |
| v70b | Session 1 admin conversation redesign: 3-column layout (340 + flex + 320), grouped time buckets (Today/Yesterday/This week/Older), unread+wait-time pills, polished thread header (avatar+name+sub+actions), day separators, avatar grouping (3+ msgs in 5min stack visually), role-distinct bubbles, viewer-relative positioning, composer with auto-resize + ⌘+Enter |
| v70 | admin-guests polish: search box (name/email/location/tier/notes), filter tabs (All/Founding/Active/New), sortable columns, default sort newest joined desc, Provider as Google/Apple/Email text, removed unused Credits column, XSS-safe |
| v69c | admin-bookings: GMV stat fixed (exclude cancelled, use totalAmount), Value column shows total + deposit ("$8,400 / $2,100 dep."), distinct empty states, auto-refresh 30s when visible |
| v69b | admin-bookings polish: real-time search across ref/name/email/property/room/notes, sortable columns (Reference/Arrival/Value) with arrow indicators, default sort newest createdAt desc |
| v69 | Admin stub cleanup: deleted admin-conversation.html, admin-property-detail.html, admin-booking-detail.html, admin-content.html, admin-invite.html. Sidebar scrubbed across all admin pages. Worker email templates updated to `admin-conversations.html` |
| v68f | Auth gating polish: admin-login.html shows yellow banner + sign-out option for already-signed-in non-admins (doesn't auto-mount Clerk widget or sign them out). SPA fallback removed (`not_found_handling: "404-page"`) |
| v68 | Admin auth gating: `assets/admin-gate.js` client gate (waits for `Clerk.loaded===true`, hides body until verified, redirects non-admins to `/admin-login.html`), worker `isAdmin()` server gate on POST/DELETE `/api/property`, `assets/admin-fetch.js` wraps fetch to attach X-Admin-Email/X-Clerk-Session. Allowlist: `admin@thebearing.io` (hardcoded in 3 places — keep in sync). Clerk allows 2nd email per account |
| v67 | Stale conversation reminder cron: hourly `0 * * * *` in wrangler.toml `[triggers] crons`. Worker `runStaleConvReminders()` scans `unreadAdmin>0 && !archived`, escalates 24h→partner, 48h→partner+admin, 72h→urgent both. Tracks `conv.reminders.{sent24At,sent48At,sent72At}`, resets on reply. Admin UI shows colored pill in thread header ("24h reminder sent" amber → 48h orange → 72h red) |
| v66h | Admin dashboard rewrite: removed deprecated Property pipeline card. Real Live Activity card (new enquiries, replies, bookings, member signups — clickable). Real Conversations needing attention (sorted oldest first, dynamic wait time, red ≥4d, amber ≥1d, "Step in" button). Recent bookings table reads `/api/booking`. Auto-refresh 30s when tab visible |
| v66 | Foundation period: pre-redesign housekeeping (admin sidebar consistency, status badge polish, various stub pages cleanup) |
| v65 | Cron infrastructure added: wrangler `[triggers] crons` + `scheduled()` handler scaffolding |
| v64 | Resend inbound webhook: `/api/inbound-email` handler. Resend `email.received` has NO body — must fetch via `/emails/receiving/{id}` |
| v63 | Notification toggles `notifyAdmin`/`notifyPartner`/`notifyGuest` on conversations; `/api/notify-toggle` endpoint |
| v62 | Conversation system maturation: presence (`/api/presence` + `conv-presence.js` heartbeat/visible/favicon-dot), unread counters recomputed, archive status |
| v61 | Clerk authentication integrated for admin login; `/api/members` member tracking, `/api/clerk-webhook` for sign-up events |
| v60 | Admin listing order tab: drag-to-reorder with `__index_cruises`/`__index_hotels`/`__index_villas` curated arrays; `toast()` UI helper added |
| v59 | Dynamic KV loaders for cruises/hotels/villas: curated index controls featured order, unmatched items appended after |
| v58 | Envoy L1-L3 work: page-aware dossier+sources injection, structured dossier admin authoring, AI-assisted dossier draft with web search. L4 Vectorize blocked. `CI_SYSTEM` prompt fragmentation documented in `docs/ENVOY_PROMPT_EXTRACTION_DOSSIER.md` for future extraction to `assets/envoy-prompt.js` |
| v57j | Cruises header: CF image `2d9c0e5f` set as hero, object-position bottom 2/3 |
| v57i | Cruises header: updated to CF image URL |
| v57h | Cruises header: full-bleed cinematic dark (Option A) applied |
| v57g | Cruises header: Option C (photo strip + text bar) applied |
| v57f | Cruises: removed 3 demo voyage cards (Aqua Nera, Four Seasons, Silver Origin) |
| v57e | Listing loaders: fixed hasCurated scope + removed double-fetch |
| v57d | Listing loaders: fixed isCruise filter blocking curated properties |
| v57c | toast() added to admin; cruises/hotels/villas loaders respect curated order fully |
| v57b | Villas dynamic loader added; all 3 listing pages have KV loaders |
| v57 | All 35 pages clean audit; NEN image URLs replaced; cruises KV loader added |
| v56f | property.html: dual Book now + Enquire buttons added |
| v56e | Vibe search: frosted glass suggestion pills |
| v56d | Mobile slide-up drawers for booking + enquiry on all pages |
| v56c | Book bar: dual buttons on all 13 property pages |
| v56b | Mobile overlays: X button + bottom sheet CSS on all pages |
| v56 | 12 property pages fully rebuilt from nour-el-nil template |

---
*Update this file every time a new build is pushed to GitHub.*
